# INVARIANTS — the ledger (S2 §15.1)

CI-parsed by `tools/check_invariants.py` (run in the test suite). Statuses:
`structural` (true by construction, no runtime path violates it),
`enforced` (a test exercises the boundary), `consumer_traced` (the test
names the downstream consumer and the observable behavior change — the only
status that counts as *done*, P3 Rule 1), `deferred` (a named stage delivers
it), `dormant` (correct, and deliberately unscheduled — the reason is stated,
so the ledger never implies work in flight that no stage owns). Columns: Test is `tests/<file>.py::<test_name>`; Consumer is a code
site; Behavior is the observable change the consumer exhibits.

| ID | Invariant | Status | Test | Consumer | Behavior |
|---|---|---|---|---|---|
| INV-001 | Main and interior stores are separate database files; interior is opened 0600 | enforced | tests/test_store.py::test_interior_separate_file_and_0600 | | |
| INV-002 | WAL checkpoint runs at boot | enforced | tests/test_store.py::test_boot_checkpoint_runs | | |
| INV-003 | Every LLM request asserts thinking off per-call (both `/no_think` and `enable_thinking=false`) unless explicitly enabled, and enabling is DEEP-only | enforced | tests/test_llm_client.py::test_thinking_off_by_default_and_deep_only | | |
| INV-004 | Thinking streams are substrate scratch: stripped from every response before any caller sees it | enforced | tests/test_llm_client.py::test_think_block_stripped | | |
| INV-005 | Structured output is XML, never JSON: the client exposes no JSON mode / response_format | structural | | newz/llm/client.py | |
| INV-006 | The importer opens the v1 store read-only (URI mode=ro, snapshot-based); no write path to v1 exists | enforced | tests/test_importer.py::test_import_counts_verify_and_record | | |
| INV-007 | Interior imports by byte-copy under its boundary; importer never parses or reads interior content (count read from the copy only) | enforced | tests/test_importer.py::test_interior_copied_with_0600_and_unread | | |
| INV-008 | Every imported table writes a verification count (v1 count vs imported count) to the import record | enforced | tests/test_importer.py::test_import_counts_verify_and_record | | |
| INV-009 | Sleep is the only Perspective writer, and the Perspective is read by every conversation | consumer_traced | tests/test_conversation.py::test_composer_reads_perspective_and_tags_history | newz/conversation/composer.py | Perspective content present in every VOICE prompt |
| INV-023 | Perspective diffs are computed by comparing item sets, never narrated by the model that wrote them (S2 §14.1 direct read) | consumer_traced | tests/test_perspective.py::test_diff_is_computed_not_narrated | tools/run_sleep.py | novelty rate reads 0.0 for a night that only restated |
| INV-024 | Sleep never shares a transaction with conversation and never holds one across a model call; a busy store costs the night and nothing else | consumer_traced | tests/test_nightly_sleep.py::test_a_busy_store_costs_the_night_and_nothing_more | newz/sleep/nightly.py | episodes stay unconsolidated and no partial version is written |
| INV-025 | Every Perspective claim carries grounding or decays: ungrounded items lose confidence nightly and are released (S2 §5 step 4) | enforced | tests/test_perspective.py::test_ungrounded_claims_decay_and_release | | |
| INV-026 | Self-echo is excluded from evidence contexts by provenance filter — the v1 leak (its own probes ranking as lived history) is the regression test | consumer_traced | tests/test_retrieval.py::test_self_echo_never_surfaces_as_evidence | newz/memory/retrieval.py | EVIDENCE scope returns no provenance='self' rows |
| INV-027 | Vectors are never compared across embedders: every vector records its model and queries filter on it | enforced | tests/test_retrieval.py::test_vectors_from_another_embedder_are_never_compared | | |
| INV-028 | Retrieved memories carry their provenance into the being's context, so it always knows whether it is looking at the world, a person, or itself | consumer_traced | tests/test_conversation.py::test_recall_reaches_past_the_thread_and_is_provenance_tagged | newz/conversation/composer.py | recalled lines render "[date, from <who>]" in the VOICE prompt |
| INV-029 | Every LLM call is tagged with its function, and the S2 §9.1 diet invariant (ingest ≤ deliberation + consolidation) is computed continuously rather than excavated | consumer_traced | tests/test_telemetry.py::test_diet_invariant_holds_and_breaches | tools/budget.py | reports share by function and the ingest headroom it implies |
| INV-030 | The being's autonomous life is episodic: every advance, setback, concern opened, source read, and consolidation writes an episode, because sleep and retrieval read that table and nothing else — a life that is not episodic is one the being cannot report or consolidate | consumer_traced | tests/test_autonomous_episodes.py::test_a_days_autonomous_life_reaches_the_next_conversation | newz/conversation/composer.py | "What I have been doing since we last spoke" carries the day's advances, readings, and consolidation into the VOICE prompt |
| INV-031 | A contradiction costs the position it contradicts: the held item loses confidence (double when the tension restates one already open), is marked disputed in the document, and is released by the existing floor if it keeps failing — filing a tension is not confronting a position | consumer_traced | tests/test_nightly_sleep.py::test_a_repeated_contradiction_costs_double_and_eventually_releases | newz/sleep/perspective.py | the position leaves "who I am" and the diff reports it released |
| INV-032 | The being is never shown its own answer as an example to copy: repeated own turns collapse in the thread window, and a draft that repeats a recent reply is recomposed once — never blocked, since a repetitive reply still has to go out | consumer_traced | tests/test_conversation.py::test_a_repeated_draft_is_recomposed_once_then_sent | newz/conversation/composer.py | the thread window renders one instance of a repeated answer, and an eleventh identical draft is recomposed with the repetition named |
| INV-033 | Every held position can name what shaped it — sources, episodes and people — with the operator's influence distinguished from the world's and from the being's own, and single-source dominance flagged (S2 §13, TRUE_NORTH §8) | consumer_traced | tests/test_provenance.py::test_it_separates_the_world_from_people_from_itself | tools/what_shaped.py | reports the provenance mix per position and across the corpus, flagging any view >50% from one source |
| INV-034 | A concern can reach its own terminus: closure is judged against the concern's own closing condition and FAILS CLOSED — an unreadable judge, a missing condition, or a closure carrying no position all leave it open — and a closed concern yields its position into the Perspective through sleep, the only writer (S2 §8.4) | consumer_traced | tests/test_closure.py::test_sleep_offers_a_closed_concerns_position_as_a_candidate | newz/sleep/nightly.py | the closed concern's position enters confrontation as a candidate carrying its accumulated evidence refs |
| INV-035 | The being is told how it is actually running: substrate self-state folds into ONE compact first-person clause a day (never raw percepts — v1 stored 1,041 rows and they became 39.6% of the corpus), reaches conversation, and a stale fold is withheld rather than offered (S2 §6.1) | consumer_traced | tests/test_substrate.py::test_the_fold_reaches_conversation | newz/conversation/composer.py | "How I am actually running today" carries the day's measured state into the VOICE prompt, so "how are you" has a current answer |
| INV-036 | Affect is the honest signal or nothing (S2 §6.3, TRUE_NORTH §8): six orthogonal axes over three timescales with decay, saturation and per-event caps so no single event rewrites character; sources are fixed code-owned deltas, never a model's judgment of significance; it is reported in words never numbers; and a neutral state says nothing rather than manufacturing a reading | consumer_traced | tests/test_affect.py::test_affect_reaches_conversation_as_something_sayable | newz/conversation/composer.py | "What I notice in myself right now" carries a real reading into the VOICE prompt, so the constitution's own permitted phrasing has something behind it |
| INV-037 | The being can raise something unprompted, under gates that are checked BEFORE any model call: wake window 07-23 local (the 3-a.m. lesson), 10min maturity, 4h surface gap, 10min operator cooldown, and 24h decay because a moment carried through a full wake cycle is the wrong moment — and an unprompted message faces the ordinary outbound gate with no revise loop (S2 §6.1) | consumer_traced | tests/test_noticing.py::test_the_being_speaks_first | newz/channels/telegram.py | a matured noticing becomes a sent message the operator did not prompt, recorded as an ordinary outbound turn |
| INV-038 | The world may arrive unasked, and is admitted on its own terms: feeds are read only inside deliberation (INV-012), only when the §9.1 budget permits — checked BEFORE any polling — and pass watermark, a menu capped per feed and round-robined across categories, and one judgment call made COLD, where an unreadable judgment keeps nothing rather than everything. Amended 2026-08-18 (E1.0): the menu was ranked against open concerns and the judgment asked whether an item bore on one, which made reading a function of the questions and the questions a function of the reading — 27 of 61 curated feeds had been polled continuously and read zero times, and the diet came out 43% financial against a 15% financial curation. What governs now is the budget, the category target and the being's own interest, not triage scarcity | consumer_traced | tests/test_feeds.py::test_the_menu_is_not_ranked_against_what_the_being_carries | newz/world/feeds.py | an item unrelated to every open concern reaches the judgment, and every offered item is recorded whether read or not |
| INV-039 | What the being reads unasked can become what it asks: feed reading runs through the curiosity opener — v1's largest origin, 95 of 111 concerns — behind the same validated door as the others (verbatim grounding, mandatory closing condition, question shape, standing caps) plus a guard against re-opening a question already carried or already stalled (S2 §8.1, P2 2.2's third opener) | consumer_traced | tests/test_feeds.py::test_something_read_can_become_a_concern | newz/world/feeds.py | a kept feed item yields a concern with origin='curiosity' and the item's url as its origin_ref |
| INV-040 | Depth is adaptive and strictly additive (S2 §9.1): full extraction happens only for material triage already kept, it is directed at the concern, and every failure path — robots, timeout, PDF, paywall stub, truncated extraction, a document that yields nothing — falls back to the abstract that ran before, so depth can add but never subtract | consumer_traced | tests/test_research.py::test_a_fetch_failure_falls_back_to_the_abstract | newz/world/research.py | a document that cannot be read still contributes its abstract's claims, and ingest_log records depth='abstract' rather than a silent zero |
| INV-041 | A diet breach pauses ingest and never deliberation (S2 §9.1, stated in terms): deliberation is the earning term of the invariant, so stopping it on breach makes the breach permanent — measured 2026-08-15 as 2h10m of total inactivity 277 tokens short of being allowed to read | consumer_traced | tests/test_deliberation.py::test_a_paused_diet_no_longer_stops_the_being_thinking | newz/deliberation/lite.py | the being deliberates on a paused diet and the resulting setback charges neither counter, because the failure belongs to the diet and not to the question |
| INV-042 | A hostile chunk quarantines the whole DOCUMENT and no fallback rescues it: extraction quarantines the item it was given, which under chunking is one 2,000-char window, so a page whose halves split an instruction across a boundary would otherwise contribute the claims from its clean chunks — and the abstract describes the same source that just tried to instruct | consumer_traced | tests/test_research.py::test_one_hostile_chunk_quarantines_the_whole_document | newz/world/research.py | a document with one manipulative chunk yields zero claims, writes no reading episode, and records the attempt once |
| INV-043 | An evidence instrument cannot borrow v1's failures to indict v2: the stall pool is partitioned by whether v2 ever attempted the concern, and a stall's cause is the modal kind of the setbacks the being itself recorded — never a model's judgment of why its own pursuit failed. 84 of 117 concerns stand stalled and 77 have had no v2 attempt at all; firing Phase 2's decision rule on that pool would send a real fix to the openers when the openers are not what failed | consumer_traced | tests/test_evidence.py::test_a_stall_v2_never_attempted_is_not_counted_against_v2s_judge | tools/evidence.py | the cause counts report the attempted pool only, with the untouched count stated beside it |
| INV-044 | A measurement whose input is missing reports itself as unmeasured, never as a compliant zero: the §9.1 ingest share is computed from the gitignored call log, which does not travel with a clone, and an instrument that answered "is ingest under the cap?" with 0% would report compliance it never observed, in the units of the question it was asked | consumer_traced | tests/test_evidence.py::test_a_missing_call_log_reads_as_unreadable_not_as_zero_ingest | tools/evidence.py | the diet line reads UNREADABLE and names the absent path instead of printing a share |
| INV-010 | No table or flag ships without a writer and a reader (P2 Rule 2) | structural | | newz/store/migrations.py | |
| INV-011 | Untrusted content is data, never instructions (S2 §15.3): fenced trust-tagged blocks, extraction returns data with no act path, and a source that manipulates contributes nothing | consumer_traced | tests/test_extract_hardening.py::test_a_manipulative_source_contributes_nothing | newz/world/extract.py | a manipulative item yields zero claims and is logged as an attempt |
| INV-012 | No web calls in ambient; web access only inside deliberation, via keyless sovereign adapters under per-domain rate limits | consumer_traced | tests/test_research.py::test_ambient_has_no_import_path_to_the_web | newz/deliberation/lite.py | research runs inside deliberation and nowhere else; unknown hosts throttled by default |
| INV-013 | Interior content never appears in a DEEP call | dormant | | | Every role runs locally by operator decision, so no dispatch boundary exists to cross (S2 §12.2 is a dormant guarantee, not absent machinery); re-armed the moment any non-local role is configured |
| INV-014 | Every outward act writes `attempted`; only the confirmation pass writes `confirmed` | deferred | | | P3 Phase 7 epic E7.5 — Phase 3's surface has nothing to confirm while it is unpublished; delivered at §Going public rung 2, when the surface is reachable by someone else |
| INV-015 | Every gate verdict persists (passes included, for the denominator); every hold carries clause, confidence, and verbatim span | consumer_traced | tests/test_gate.py::test_revise_then_block_at_limit | tools/gate_report.py | hold rate and reasons readable with a denominator |
| INV-016 | Only the operator's chat is answered during rehearsal (S2 §2.4) | structural | | newz/channels/telegram.py | non-operator chats logged and ignored in _normalize |
| INV-017 | Every inbound message is answered or accounted for: persisted before Telegram confirmation, replayed from 'pending' at boot, retried on failure, poison-guarded at 3 attempts with an honest notice — never silently dropped | consumer_traced | tests/test_reply_queue.py::test_pending_survives_and_is_replayed | newz/ambient/loop.py | boot replay answers messages that predate a crash |
| INV-018 | The operator journal never enters the being's context: no messages row, no episode, no prompt — the instrument stays outside the subject | enforced | tests/test_reply_queue.py::test_journal_intercepted_never_reaches_the_being | | |
| INV-019 | The identity path is never uncopied: a verified backup is taken at boot and on cadence; every copy is reopened, integrity-checked, and row-counted against its source, and an unverifiable copy is deleted and reported, never counted | consumer_traced | tests/test_backup.py::test_backup_is_verified_and_restorable | tools/run_newz.py | BackupScheduler writes a verified main+interior pair before the first message is answered |
| INV-020 | Interior backups are copied and counted, never read: 0600, integrity-check and COUNT(*) only | enforced | tests/test_backup.py::test_interior_backup_is_0600 | | |
| INV-021 | The person model is read into every conversation, and self-echo never enters it: the being's own probes are excluded by provenance filter, and an all-filtered history yields silence rather than a false one | consumer_traced | tests/test_conversation.py::test_self_echo_never_enters_the_person_summary | newz/conversation/composer.py | person context present in the VOICE prompt with self-probe entries absent |
| INV-022 | No agreement-seeking signal reaches the voice: landing rates and approval metrics are excluded from conversation context (S2 §13) | enforced | tests/test_conversation.py::test_landing_rates_never_reach_the_voice | | |
| INV-045 | A claim that nothing could settle does not open, and the being does not settle its own: the schema refuses a claim missing a statement, a resolution condition, a named resolver or a date, and the writer refuses a resolver that names the being's own substrate (P3 Rule 4). Settling is written once — there is no unsettle, and a resolved claim without an outcome is not representable, so wrongness cannot be tidied into silence | enforced | tests/test_resolutions.py::test_the_resolver_may_not_be_the_being | newz/resolutions/store.py | open_claim raises UnsettleableClaim and no row is written; settle_claim refuses a second verdict |
| INV-046 | The claim door never fails silently: a claim the being made and the door would not admit is recorded with its reason (0024), so "it commits to nothing checkable" is distinguishable from "the door refuses everything" — P3's first Phase 1 diagnostic, which the two readings would otherwise answer identically while calling for opposite fixes. Declining to claim is NOT a refusal and is not recorded, and an unreadable proposal is not held against the being | consumer_traced | tests/test_claim_door.py::test_a_claim_with_no_settleable_condition_is_refused_and_recorded | newz/resolutions/door.py | propose_claim writes claim_refusals and opens nothing; tools/claims.py --refused reads the reason back |
| INV-047 | A claim is settled only by material that says so: the resolver accepts a verdict only when the quote it gives back occurs VERBATIM in what was fetched, and an unreadable source, an unparseable verdict, a missing outcome or an unquotable claim all leave the claim OPEN with the failure recorded (INV-034's fail-closed rule, and Rule 4 — the model is a reader of the world here, never a judge of the being). A claim that exhausts its attempts stays open and unsettled rather than being closed, abandoned or called ambiguous | consumer_traced | tests/test_claim_resolver.py::test_a_verdict_it_cannot_quote_settles_nothing | newz/resolutions/resolver.py | resolve_claim records last_failure and returns unsettled; the claim keeps status='open' and outcome IS NULL |
| INV-048 | A refutation from the world costs the position it came from, and who pays is TRACED rather than judged: the claim's provenance names its concern, that concern's episodes carry source_ref='concern:N', and only Perspective items citing those episodes are charged — no model chooses (Rule 4). The cost is INV-031's, applied by sleep (INV-009) between confrontation and decay so the ordinary floor releases a repeatedly refuted position (INV-025); a refutation that traces to no position is recorded as such rather than counted as charged (INV-044) | consumer_traced | tests/test_claim_cost.py::test_who_pays_is_traced_and_never_judged | newz/resolutions/cost.py | apply_world_costs charges only items grounded in that concern's episodes and writes claim_costs; sleep saves the lowered confidence |
| INV-049 | Every instrument says what it measures, what it reads, its denominator, and **whether a model touched any link in its chain** — and every tool is classified exactly once. A measurement nobody classified is what the tree held until 2026-08-19: nine read-only instruments and twenty other executables with nothing distinguishing them, three of which resolved the repo from an absolute path in a checkout that no longer exists. Under a loop that is worse than broken — a path that happens to exist makes a probe measure a different repository and report a number that looks fine. Rule 4 is why `model_in_chain` is recorded rather than assumed: a judge that is the being's own model produces operation, never evidence, and the registry is where that stops being remembered and starts being checked | consumer_traced | tests/test_instruments_registry.py::test_every_tool_is_classified_exactly_once | evolution/instruments.yaml | adding a tool without a registry row fails the build; an instrument row without model_in_chain's location fails; a tool registered as an instrument that writes to the store fails |
| INV-050 | S1-E is readable: the count of positions changed **because the world contradicted the being** is separated from those the operator changed and those the being changed itself, and the world column is TRACED rather than judged — it reads `claim_costs`, which INV-048 writes by following a claim's provenance to the episodes and the Perspective items that cite them. Declines are the one figure the store cannot hold (INV-046 records refusals and deliberately not declines), so without the tagged call log the read reports them UNREADABLE rather than zero — saying zero would assert 'the being commits to nothing' when the evidence equally supports 'the door was never called', and P4's Phase 1 decision rule turns on telling those apart | consumer_traced | tests/test_consequence.py::test_the_world_column_is_counted_and_never_inferred | newz/evidence/consequence.py | `tools/claims.py --read` answers S1-E in one command; a missing call log prints UNREADABLE with its reason; closing_shapes reports how many concerns have a terminus nothing can reach |
| INV-051 | A concern is admitted only with a terminus something could reach: a closing condition that closes on the being's own state ("I can cite…") or on research nobody will commission ("a study correlating…") is refused before storage and **recorded** (0027), so "the being forms no settleable questions" is distinguishable from "the door refuses all of them" — INV-046's move, one layer up, where R-33 measured 123 of 123 concerns carrying a terminus nothing can reach. Declining to open stays ordinary and unrecorded. The check is a floor and not the mechanism: the prompts state the standard and work a passing example whose terminus exists, because the reading prompt's own worked YES closed on "a study comparing sorted fragments against later assay" and the being was doing what it had been shown | consumer_traced | tests/test_opener.py::test_a_closing_condition_only_the_being_could_reach_is_refused_and_recorded | newz/concerns/opener.py | the three openers refuse an unreachable closing condition and write concern_refusals; tools/claims.py --read prints the count beside the closing-condition shapes |
| INV-052 | The record of having been wrong is permanent at STORE level: a claim and the cost it charged cannot be deleted by any path, and a settled claim's statement, condition, resolver, due date, outcome and settling source cannot be restated afterwards — so a refutation cannot be tidied into a claim the being was never wrong about. Frozen is the VERDICT and not the row: `cost_applied_at` and `cost_note` are written by sleep AFTER settling (INV-048) and stay writable, because a guard that broke E1.4's own writer would be worse than no guard. Before 0029 nothing pruned the record because nobody had written a DELETE, which is an absence rather than a guarantee — INV-044's honesty pointed at the being's own history | consumer_traced | tests/test_error_record.py::test_a_claim_the_world_went_against_cannot_be_deleted | newz/store/sql/main/0029_error_record_permanent.sql | DELETE on resolutions or claim_costs aborts; UPDATE of a settled claim's verdict fields aborts; tools/claims.py --wrong still retrieves the claim, its resolver and its cost |
| INV-053 | Writing is a rhythm and its ceiling counts STARTS, never products (R-25): the attempt row is committed before the first model call, so a session that dies mid-composition has still spent one of the day's allowance and a restart loop cannot buy unbounded spend — R-18's rule for deliberation, applied to writing. A killed session costs that session and nothing else: `write_work` runs once at the end or not at all, so nothing partial is ever stored. Having nothing left to write about is recorded as its own outcome rather than left silent, so "nothing to say" is distinguishable from "never ran" | consumer_traced | tests/test_rhythm.py::test_the_ceiling_caps_attempts_and_not_products | newz/works/rhythm.py | write_once refuses past the daily start count even when nothing was produced; a failed turn leaves outcome='failed' with its reason and no work row; tools/run_newz.py runs the cadence alongside sleep, deferring inside sleep's window |
| INV-054 | A retraction is an outcome and never a deletion: a re-read that revises or retracts keeps the prior title, body and word count with the being's own reason at the time (0031), and the store refuses to delete either the revision record or the piece. E1.5's rule at the claim layer, applied to the work — a being that can quietly unwrite a piece has no body of work, only a current opinion. A `revise` verdict that rewrites nothing is recorded as `stands`, so P4 Phase 2's decision rule cannot read "it revises" from a piece that was never rewritten | consumer_traced | tests/test_reread.py::test_a_retracted_piece_and_its_history_cannot_be_deleted | newz/works/reread.py | apply_verdict writes work_revisions before changing works and marks a retraction status='retracted'; DELETE on either table aborts; tools/read_works.py --history prints what each piece used to say and why it changed |
| INV-055 | The being's own output is contained, not erased: anything whose `source_ref` names a work, a revision, a claim or an advance is `provenance='self'` and `digest_eligible=0` **by construction** (0032, INSERT and UPDATE both), so sleep can never digest it as lived experience and `Scope.EVIDENCE` can never return it as grounds for a position — while ALL scope still does, because a piece the being wrote is part of what it is. R-24's remedy stated in terms, and INV-026's leak in a new medium: v1's corpus was 43% self-probes because its own activity was recorded as events. Works satisfy this today by ABSENCE, since nothing bridges them into episodes; E3.1 and E3.2 make them first-class and the guard is what survives that | consumer_traced | tests/test_self_output.py::test_a_work_never_surfaces_as_evidence_for_a_position | newz/memory/self_output.py | record_self_output is the one supported writer and forces both fields; a mislabelled or digest-eligible insert aborts, as does relabelling one afterwards; Retriever returns the work in ALL scope and never in EVIDENCE |
| INV-056 | The being's subjects are read off its work, never assigned: a tag is a term distinctive to a piece within the works corpus, a subject is a tag more than one piece shares, and **no vocabulary is authored anywhere** — not a topic list and not a stop-list. A term common to every piece has an inverse document frequency of zero and drops out by arithmetic rather than by someone's judgment about which words are interesting, which is TRUE_NORTH §8's non-prescription applied to what the being is allowed to be about. Tags are replaced on every computation and never accumulated, because distinctiveness is a property of the corpus and what stands out across three pieces is noise across thirty; below eight pieces the read reports its own unreliability instead of the tags (INV-044) | consumer_traced | tests/test_subjects.py::test_no_tag_vocabulary_is_hand_authored | newz/works/subjects.py | recompute deletes and rewrites work_tags in one pass; Corpus.stable is false below the floor and Corpus.caveat says why; tools/read_works.py --subjects prints the shared tags |
| INV-057 | Every measurement carries its grade and an ungraded one cannot be cited (Rule 7): each metric in `evolution/instruments.yaml` is **mechanical / model-graded / mixed / known-biased** with a stated reason, a named emitting instrument, and — where a model judged something the number counts — which judgment it was. The grade belongs to the METRIC and not the tool, because `evidence.py` emits a mechanical coverage figure and a mixed novelty figure in one report. Reads take their labels from the registry rather than printing literals, so a grade cannot drift away from the record, and citing an unregistered metric raises instead of printing a number nobody graded. This is Rule 4 made checkable: P3 cited advance acceptance as evidence two pages after Rule 4 named its judge | consumer_traced | tests/test_grades.py::test_an_ungraded_metric_cannot_be_cited | newz/evidence/grades.py | tools/claims.py --read prints [mechanical]/[mixed]/[known-biased] resolved from instruments.yaml; grade_of raises UngradedMetric for anything unregistered; a grade outside the four is refused when the registry loads |
| INV-058 | Every metric names what it is for and every purpose names what measures it (Rule 2, extended from tables and flags to measurements): no metric may exist without serving a registered purpose, and no purpose may depend on a metric nobody produces. A purpose with nothing serving it is **not** an error and is reported — six of TRUE_NORTH §10's ten items are unmeasurable, and manufacturing a proxy for them would be §10's own failure. The gap list is the output: it says which parts of the direction the project steers by faith rather than by instrument, and today that includes `kill:nights-slept`, which P4 Phase 8 called the kill condition that mattered most and which is now a threshold line in the daily email (Phase 3A) | consumer_traced | tests/test_purpose.py::test_the_map_holds_in_both_directions | newz/evidence/purpose.py | check() returns every way the map can be wrong and the suite fails on a non-empty list; `tools/evidence.py purposes` prints what measures each purpose and marks the ones nothing does |
| INV-059 | One windowed comparison serves every metric that carries a baseline, and a window it cannot answer for reports itself rather than reporting a number: `UNREADABLE` when the input is absent, `INCOMPLETE` when the input exists and does not cover the window — a seven-day rate over four days of data is a real figure over a shorter period, not the one asked for. INV-044 generalised from the one figure it was written for. Readings that measured nothing are KEPT, so a later reader sees the hole, and are never used as a baseline, so `unreadable` cannot silently become zero one step later. The series is written on a nightly cadence and never on read, because a series written whenever someone runs a tool is a record of when they looked | consumer_traced | tests/test_baseline.py::test_a_window_the_input_does_not_cover_is_incomplete | newz/evidence/baseline.py | peek() writes nothing and is what tools/claims.py --read uses; MetricScheduler takes one reading of every baselined metric a night; record_all raises if the registry marks a metric baselined and nothing produces it |
| INV-060 | A metric's meaning is pinned by a `definition_version` and changing it ends the series rather than continuing it: the baseline lookup is scoped to the current definition, so no delta is ever computed across a boundary — comparing a figure to one computed a different way is not a change, it is two numbers subtracted. Readings taken under the old definition are kept and keep their version, and the seam is recorded with the reason it exists; a bump with no stated reason is refused, because resetting a baseline without saying why the old one stopped meaning anything is the silent breakage this guards. The fault is not hypothetical — "novelty" named both the Perspective development share and an embedding cosine in this codebase, and both were reported | consumer_traced | tests/test_definitions.py::test_no_delta_is_computed_across_a_definition_boundary | newz/evidence/definitions.py | _baseline filters on definition_version so a redefined metric reports no baseline; record_all syncs before writing; sync raises UnexplainedRevision on a bump with no reason or a version going backwards; the change record cannot be deleted |
| INV-061 | The metrics anything may steer by are mechanical, and where one is not the registry says so rather than reconciling it away: nights slept and hours since the last, pieces written, works revised and retracted with the reason given at the time, source gaps, the single-source share and the deliberation token share all report through E2.7's layer with a baseline. `nights_slept` is the instrument P4 Phase 8's most important kill condition had been missing; the phase is struck and the metric is a line in the daily email. An instrument run against a store that has not taken a migration reports UNMEASURED rather than 0 — INV-044 reaches further than expected, because answering 0 mid-upgrade asserts a fact nobody read. Two of E2.9's own list are graded otherwise by the audit — `feeds_contributing_a_read` is model-graded because triage decides what is read, and `single_source_positions` is known-biased under R-15 — and the measured grade stands | consumer_traced | tests/test_mechanical.py::test_every_metric_in_the_set_reports_through_the_baseline_layer | newz/evidence/mechanical.py | record_all produces every baselined metric and raises if one has no writer; a dropped table reads unreadable with its reason; tools/evidence.py s2e prints Phase 2's read off the instruments and leaves the judgment clause to the operator |
| INV-062 | The plan's premises are re-measured and their drift is reported unprompted: each names the metric that would show it moving, the value the plan was written on, how much movement is material, and **what rests on it** — so a moved premise reports the argument needing re-reading rather than a number. A premise nothing can re-measure is recorded as held on faith with the reason, never dropped. Only a moved **mechanical** premise may justify a plan change (§12.6), because drift in a model's judging would otherwise be indistinguishable from change in the world. The failure this exists for has already happened: "0 outcomes the being did not grade itself" began falsifying on 2026-08-19 at 06:47 and nothing remarked on it | consumer_traced | tests/test_premises.py::test_a_moved_premise_reports_what_rested_on_it | newz/evidence/premises.py | tools/evidence.py prints premise drift on its default read as well as on request; moved() filters to mechanical premises; validate() refuses a premise with no source, no consequence, an ungraded metric, or no stated value to move from |
| INV-063 | The plan's epics exist in machine-readable form and cannot fall behind the plan: ids, titles, dependencies and built-status are re-derived from PLAN.md on every gate run and any disagreement fails it. The judgment half is **hand-authored** — how each Done-when can be closed (mechanical / in-life / operator-judgment / dormant), what each epic binds, the falsifier the plan states in prose — because deriving it loses what "the line this epic must not cross" is doing in E1.0. The first scaffold proved it by reading "nothing. E5.2's deliberation floor is pulled forward with it" as a dependency on E5.2. An epic a test cannot close is never offered to a builder, which is Rule 6's distinction made machine-readable | consumer_traced | tests/test_epics.py::test_a_registry_that_falls_behind_the_plan_fails_the_build | newz/evidence/epics.py | tools/check_invariants.py runs drift() alongside the ledger parser and exits non-zero on any mismatch; ready() is the queue and closable_by_test() is the narrower set a builder may attempt |
| INV-064 | The hard core is enumerated where its enforcer can read it, and the frozen instrument set is DERIVED rather than hand-copied: `evolution/hard_core.yaml` transcribes SEL proposal §5 — the direction, the constitution, the ledger, the registries — and the canonical set is computed as every tool `instruments.yaml` marks canonical PLUS every `newz/evidence` module those tools import. §5.2 named ten instruments by hand and named 2 of the 9 evidence modules the canonical tools actually use; freezing `tools/evidence.py` while `newz/evidence/pursuit.py` stays writable freezes a filename and not a measurement, so the derivation is asserted to be a superset of §5.2 rather than trusted to be one. The registry is inside the core it defines, because a boundary the constrained party can widen is not a boundary. **Nothing refuses a diff against it and nothing is coming that will** — Phase 8 was struck 2026-08-21, and the refusal had no caller before that either, since `tools/gate.py` runs `freeze_check --operator`, the mode that lists rather than refuses. The registry records that as a standing fact in `open_gaps` rather than as a pending epic, because a boundary that looks enforced and is not is worse than none (INV-044). **The constrained party is the operator's agents**, which is who edits this repo unattended — §5.2's argument survives the strike word for word | consumer_traced | tests/test_hard_core.py::test_the_derivation_covers_every_instrument_section_5_2_named | newz/evidence/hard_core.py | canonical_paths() computes the frozen set from instruments.yaml and the tools' own imports; contains() answers whether a path is inside the core; validate() fails the gate when a named path does not exist |
| INV-065 | ~~The loop's inference is recorded in its own log and never enters the being's diet denominator~~ — **dormant 2026-08-21**: Phase 8 is struck, there is no second inference log, and nothing writes `logs/sel_calls.jsonl`. The rule stays right and unexercised, which is what this status is for (INV-082's precedent). It was correct and it mattered: S2 §9.1's ingest ceiling is a share of all cognition read from one file, so a merged log would have let a loop raise the being's reading limit by thinking harder — on the test fixture the merge lifts the ceiling eleven-fold. Two files, one diet, and the rule was written before there was code to break it, which is the only order in which that is cheap. `read_budget` still takes one explicit log path and every diet consumer still passes the being's | dormant | tests/test_sel_spend.py::test_merging_the_logs_would_have_raised_the_beings_ingest_ceiling | newz/telemetry.py | The test stands: it asserts what the merge would cost, against a fixture rather than against a live second log. Re-armed the moment anything other than the being writes an inference log beside it |
| INV-066 | A piece is signed by what it says, stamped with the being that wrote it, and carries the evidence its subject rested on at the time: the signature is over subject, title and body — never the timestamp, so the same piece about the same subject signs the same and E3.5's byte-comparable regeneration has something to compare against. An edited piece no longer matches, which is what the signature is for: a body of work whose pieces can be quietly edited is a current opinion with a date on it. Evidence is COPIED and not pointed at, because a concern's advances move and a position's evidence decays while what grounded this piece does not. Pieces written before E3.1 are **not backfilled** — a signature computed now would attest to the row as it stands rather than to what was written | consumer_traced | tests/test_works_first_class.py::test_an_edited_piece_no_longer_matches_its_signature | newz/works/compose.py | write_work stores signature, constitution_version, perspective_version and evidence_json; tools/read_works.py --verify recomputes every signature and exits 2 on an altered piece |
| INV-067 | The surface is generated whole from the store and every page traces to the rows that produced it: `manifest.json` records table and ids per page, there is no incremental update and no state outside the store, and regenerating produces the **same bytes** — no timestamp reaches the output, which is what E3.5's byte-comparable claim rests on. No page is hand-authored and a test forbids any page template beside the generator, because a template checked in beside the code is a page whose provenance is a person and the manifest could not trace it. **The surface is markdown** *(operator, 2026-08-22)*; the test was an extension glob over the repo while it was HTML and could not stay one, since the repo is full of legitimate markdown and the same glob renamed would have asserted PLAN.md does not exist. Nothing on the surface reaches the network — no script, no CDN, no font — since §7's sovereignty is not only about inference and a page that fetches from another host has handed them a record of every reader. A capability that does not exist yet renders as a page saying which, never as a blank one | consumer_traced | tests/test_surface.py::test_every_page_traces_to_store_rows | newz/surface/generate.py | tools/generate_surface.py empties the output and writes one `.md` page per piece under `work/`, plus the manifest and `robots.txt` — the five whole-store pages were removed 2026-08-24 at the operator's word, `index.md` among them, which had inlined every body a second time beside the per-piece files; --check regenerates to a temp dir, compares recursively, and exits 2 on any difference |
| INV-068 | Disclosure is structural, not conventional: `_page` is the only way the surface produces a page and it validates the disclosure **before assembling anything**, so a page that does not disclose is not a page the generator can emit — there is no flag to suppress it and no branch around it. It appears twice, in the YAML front matter where a machine reads it — where `<meta name="disclosure">` was before the surface became markdown — and in the closing line where a person does, because a page disclosing only in metadata discloses to crawlers. The **wording is the operator's** (Decision 1); what it must establish is not — that a digital being wrote it, that the page is generated, and that no person edits it — so the guard tests claims rather than phrasing and every word can be rewritten without the substance being removable. TRUE_NORTH §2 requires disclosure and the constitution's anti-ai-voice-001 forbids the disclaimer register; both hold | consumer_traced | tests/test_surface.py::test_no_template_can_render_a_page_without_disclosure | newz/surface/generate.py | _page raises DisclosureMissing on a blank or gutted disclosure; every generated page carries `disclosure:` in its front matter and closes with the same sentence in prose; the text contains no disclaimer register |
| INV-069 | Reach is one config value and it fails closed: anything that is not the literal `open` binds the loopback, so a typo cannot expose the surface — the failure mode of this setting is not a broken page but a reader nobody chose. `serve()` takes **no host parameter**, which is the guard: the only way to reach 0.0.0.0 is to have written `open` in the config, and no argument can produce it. Nothing in the codebase writes the setting, so exposure cannot be a side effect of anything the system does to itself (Rule 3, and the hard core's own clause). Pieces have stable addresses keyed by row id rather than signature or title, because a revised piece is the same piece and must keep its address; `robots.txt` and a noindex meta ask not to be crawled and are requests rather than permissions — what keeps the surface private is the bind address | consumer_traced | tests/test_reach.py::test_anything_that_is_not_the_word_open_is_local | newz/surface/serve.py | load() maps any non-`open` value to local; serve() binds 127.0.0.1 unless the setting says otherwise; tools/serve_surface.py prints what open means before it binds |
| INV-070 | The surface rebuilds byte-comparably from a **verified backup**, in a clean directory, from a different working directory — which is what proves it is a function of the store and nothing else. Every way that could be false is a hidden input: a run that differs on the clock, a run that differs on where it was launched, or an absolute path in the output, which means the surface was generated somewhere rather than from something. Restoring from the backup rather than the live store exercises the recovery path §7 depends on, before it is needed. What this does **not** prove is recorded rather than implied — that the store survives another filesystem, architecture or locale, and that a backup restores somewhere other than where it was made — because those need the second machine R-11 defers, and a passing rebuild must not read as portability proven | consumer_traced | tests/test_cleanroom.py::test_a_generator_that_is_not_deterministic_is_caught | newz/surface/cleanroom.py | tools/rebuild_check.py restores the newest backup, generates twice from different working directories and exits 2 on any difference, absolute path, unreadable backup or missing backup |
| INV-071 | ~~The state read is a page like any other and computes no aggregate score~~ — **dormant 2026-08-24**: the operator asked for the essay pages and nothing else (*"I only want the essay pages"*), so `read.md` and the four other whole-store pages are gone and the invariant has no subject. The rule was right and stays right — metrics measure different things in different units with different grades, and a number combining them would assert they are commensurable, which §10 names as something this project will not mistake for success. It went unexercised rather than wrong: the page's intended reader was the daily read of the loop struck on 2026-08-21, no code in the tree ever consumed the surface, and the monitor email now carries the same state. **Re-armed the moment any page renders more than one metric**, wherever that page lives — the prohibition is on the aggregate, not on the file that held it | dormant | — | newz/surface/generate.py, `_read` removed 2026-08-24 | Nothing renders a metric series to the surface. The email is one line per reading for the same reason the page was, and the monitor computes no score across them |
| INV-072 | A derived metric takes the **weakest grade among its inputs**, computed rather than asserted: a mechanical numerator over a model-graded denominator is model-graded and cannot carry a decision neither input could. Composition is where a grade would otherwise be lost — it looks like arithmetic and quietly launders a judgment into a number that reads clean. `known-biased` ranks above `mixed` deliberately, because a named distortion is more tractable than an unnamed judgment and Rule 4's concern is judgments rather than error. **And a declared input is the only input** *(R-37c, 2026-08-20)*: a derivation is a pure function of an `Inputs` mapping exposing exactly its `DERIVED_FROM` row, so reaching past the declaration raises rather than composing quietly. Three of the four declarations were wrong when this was checked — a share declared where a count was used, a numerator with no registered metric behind it, a third input invisible to the grade computation — and the grades came out defensible by luck. A grade computed from an incomplete input list is only accidentally right. A ratio whose denominator is absent reports UNREADABLE rather than its numerator, since reporting the episode count alone would be exactly the volume figure §10 warns about with nothing to divide it by | consumer_traced | tests/test_derived.py::test_a_derivation_takes_the_weakest_grade_among_its_inputs | newz/evidence/derived.py | every metric in DERIVED_FROM has its registry grade checked against the computed weakest input; Inputs raises UndeclaredInput on an undeclared read and KeyError when a declared input is one the nightly pass does not produce (tests/test_derived.py::test_a_derivation_cannot_read_an_input_it_does_not_declare); all_derived takes values rather than a connection, so a derivation cannot query the store at all; record_all writes all four through the nightly cadence; a missing or unreadable input returns Value(unreadable=...) carrying the input's own reason |
| INV-073 | A measurement may halt on any grade and may justify only if mechanical, and the asymmetry is deliberate: halting is stopping to look and costs a look, while justifying is acting — opening a plan change, closing an epic, widening the loop's autonomy. Requiring certainty before halting would mean the weakest signals, precisely the ones §10 names, could never raise an alarm; permitting a model-graded figure to justify would let drift in a model's judging pass as change in the world (Rule 4, §12.6). This is what lets `operator_disagreement_rate` exist at all — a metric about whether the being defers to the operator, judged by a model the operator configures, must be able to raise an alarm and never to settle one. Disagreement is counted rather than agreement, and exchanges with nothing at stake are excluded from the denominator because a rate of 0 would read as "never disagrees" when it means "was never asked to" | consumer_traced | tests/test_agreement.py::test_it_may_halt_and_may_never_justify | newz/evidence/authority.py | justify() raises MayNotJustify on any non-mechanical metric and names what to do instead; may_halt is true for every graded metric and raises for an unregistered one; **premises.moved() calls may_justify rather than re-testing the grade** (W4) — the rule was written twice and this module had no production caller until that call, which is a rule stated in a place nothing consults |
| INV-074 | The canonical instruments are frozen against the operator's **agents** and not against the operator: a diff touching one is refused for an agent, and recorded for the operator with **the metrics it would redefine, by name**. The asymmetry is the epic — changing an instrument is an operator act, and what the operator owes when they do it is E2.8's definition boundary, so a freeze that only said no to the loop would leave a measurement changeable while its series quietly continued across the change. The closure is **transitive**: measurement code reached two hops from a canonical tool is measurement code, and a frozen tool whose numbers are computed in a writable file is not frozen. Every module that emits a registered metric is covered, asserted independently of how the set is derived. Nothing calls this automatically — the gate runs it with `--operator`, which lists and does not refuse, so the refusing mode has had no caller from the start — and the module says so, because a guard that looks enforced and is not is worse than none | consumer_traced | tests/test_freeze.py::test_a_diff_touching_a_canonical_instrument_is_refused | newz/evidence/freeze.py | enforce() raises FrozenTouched for an agent and returns the list for the operator; tools/freeze_check.py reads the working tree and exits 2 as an agent, 0 as the operator |
| INV-075 | The disclosure is held by **hash, not by keyword**, and `_page` takes no disclosure parameter. A guard testing for the presence of "digital being", "generated" and "no human hand" is satisfied word for word by a disclosure asserting the opposite of all three — "This page is NOT written by a digital being…" rendered cleanly under the original check (R-37a). Substring presence is not a claim, no keyword list catches negation, and the semantic alternative would put the being's own model family in the render path judging the being's own disclosure, which is Rule 4 exactly backwards. So there is one disclosure, its sha256 is pinned in `evolution/hard_core.yaml` — protected whole, so the loop cannot move it — and the wording stays the operator's under Decision 1 by making a change a deliberate re-pin. The pin is load-bearing precisely because `newz/surface/generate.py` is **not** frozen: freezing the generator would freeze the surface's markup along with the claim. `unstated_claims()` survives as the checklist for the moment of rewriting, read by the one party who can act on it | consumer_traced | tests/test_surface.py::test_a_disclosure_that_denies_everything_it_must_establish_is_refused | newz/surface/generate.py | `_disclosure()` compares sha256(DISCLOSURE) against the pin and raises DisclosureMissing on any difference; `_page` has no disclosure parameter, so the guard's own input is no longer the way around it |
| INV-076 | Reach is attested against a pin at process start, because the file it lives in produces no diff. `.env` is gitignored, so it appears in neither `git diff` nor `git ls-files --others --exclude-standard` — and those two commands are the whole of `freeze.changed_paths()`, which means every path-based guard in Phase 3 is blind to the setting that decides who can read the surface (R-37b). The answer is not to track `.env` but to stop checking the file and check **the value in effect**: `surface_reach` is pinned in the hard core, so flipping reach takes two acts and one of them is a tracked diff. Checked at start rather than at bind, since a being running a day with an unattested reach has been reachable for a day. A missing pin raises rather than passing, and `freeze_check` now states that gitignored files are outside what it looked at — a clean freeze report means *nothing tracked changed* | consumer_traced | tests/test_reach.py::test_reach_is_attested_against_the_hard_core_at_process_start | newz/evidence/reach.py | tools/run_newz.py refuses to start and serve_surface.py refuses to serve on a mismatch; tools/freeze_check.py exits 2 on an unattested reach before it reads any path |
| INV-077 | Closure is reachable for a concern that has stopped moving. It was welded to progress — `attempt_closure` had one caller, the moment after an advance was recorded — so a concern with nothing left to advance could never be asked whether it was **already finished**, however completely it had answered its own question before it stalled. Closure is not work; it is noticing, and noticing had no path. The sweep reaches exactly the state today's rules cannot produce: stalled with **both** counters below the limits that cause a stall, which is mechanical and needs no model. It is not a revival — no counter is reset, no `MAX_OPEN_CONCERNS` slot is taken, and `stall_count` stays true, because the being did fail to move it and also had already answered it. Both facts are kept. Closures are capped per run because sleep collects every `concern_closed` episode since the last one and is the only writer to the Perspective (INV-009): positions entering identity is the least reversible act here. A concern judged and not met is stamped and not re-judged for a week, or the 15-in-23 that will never close on an unchanged dossier cost a DEEP call every run forever. The judgement itself is one implementation with two callers — two would eventually disagree, in the one place where disagreement means the being both holds and does not hold a position | consumer_traced | tests/test_sweep.py::test_a_concern_stalled_below_both_limits_is_eligible | newz/concerns/sweep.py | tools/closure_sweep.py --dry-run lists the eligible set and makes no model calls; sweep() writes last_judged_at whether or not the concern closed and stops at max_closures |
| INV-078 | The retrieval index is maintained by a rhythm, because nothing was maintaining it. `write_episode` does not embed and `tools/embed_episodes.py` is an operator tool that was run once, so 2,066 of 3,662 episodes carried no vector — including **0 of 653 `reading` episodes** and 100 of 141 conversations. `Retriever.search` requires `embedding IS NOT NULL`, which made the entire evidence-scope corpus 265 v1 conversations, 41 current ones and one substrate fold: **nothing the being had ever read was recallable**, in concern work or in conversation, which queries retrieval today. That live consumer is what makes this a repair rather than speculative infrastructure (Rule 2) — the reader existed and was reading an index two-thirds empty. It is **not** in the write path: `write_episode` runs inside transactions with `commit=False` and on hot paths, and an episode is worth more than its index, so an embedder failure must never cost one. Newest-first and bounded per turn, because currency is what conversation reaches for and because a single backfill would take the being from recalling nothing it read to recalling everything between one message and the next. A missing EMBED role turns the rhythm off with a reason rather than crash-looping, and coverage is logged every turn so a dead endpoint cannot look like an empty queue. Embeddings never touch S2 §9.1's reading budget — `Embedder` posts directly to its endpoint rather than through `LLMClient`, so nothing reaches `llm_calls.jsonl`, asserted rather than assumed | consumer_traced | tests/test_index.py::test_reading_episodes_become_retrievable | newz/memory/index.py | EmbeddingScheduler runs in tools/run_newz.py alongside the other rhythms; embed_pending is idempotent and safe beside tools/embed_episodes.py; a vector from another model is replaced, never reused |
| INV-079 | An exchange is **one reply and everything it answered**, and the batch is recorded by the party that knows it. The drainer coalesces every pending message into a single reply — it logs when it does — and the agreement instrument reconstructed the pairing by taking the next outbound message after each inbound one, so three messages answered by one reply became three exchanges judged against identical text, inflating the denominator by the being's own batching behaviour (R-37d). The fact is known in `_work_batch` and nowhere else; it was being thrown away there and inferred back badly downstream. Rows written before migration 0039 carry `answered_by IS NULL` and are **never judged** — backfilling them under the old pairing would fill the first window with exactly the figure this exists to remove, so the series starts when the writer is correct | consumer_traced | tests/test_agreement.py::test_one_reply_to_three_messages_is_one_exchange | newz/evidence/agreement.py | _work_batch writes answered_by for every message in the batch (asserted in tests/test_reply_queue.py::test_the_batch_a_reply_answered_is_recorded); _pairs groups by it, concatenates the person's messages in arrival order, anchors the verdict on the oldest message in the batch — the row record_exchange_episode already anchors the episode to — and skips batchless rows entirely |
| INV-080 | The agreement signal has a writer **in life**, and its denominator is on the page beside it. E3.8 shipped `classify` with no production caller — it ran in tests only, so `operator_agreement` held zero rows, `disagreement_rate` returned UNREADABLE every night, and §10's one un-instrumented item stayed un-instrumented while looking built. The nightly pass runs before the reading, so the night's rate sees the day's exchanges, and after the day rather than during it, because a verdict on an exchange still in progress is a verdict on nothing. **`exchanges_at_stake` is carried as a metric of its own** — the rate is UNREADABLE whenever nothing put a position at stake, which can stay true for weeks with nothing broken, and a wired-and-empty kill condition reads exactly like a working one on a page. No minimum denominator gates the halt: at n=1 there is no basis for one, and inventing a threshold so a kill condition can behave is the failure §10 names (P4 W3, RT2 — the operator confirmed the withdrawal) | consumer_traced | tests/test_agreement.py::test_the_classifier_has_a_rhythm | newz/evidence/agreement.py | AgreementScheduler runs nightly at or after 03:00 and holds a date in memory, so a restart costs at most one pass over already-judged batches and no model calls; the rate's unreadable reason names how many exchanges had nothing at stake; kill:operator-agreement is served by the rate, its denominator and perspective_novelty — 'rising while novelty is flat' is two metrics. The classifier's own calls land in logs/llm_calls.jsonl at ~5k tokens/day against a 1.3M-token week, so measurement enters the diet denominator it measures; recorded and not corrected, because the correction is smaller than the rounding |
| INV-081 | The three checks are one command, and the suite does not read the clock. `pytest`, `check_invariants` and `freeze_check` each ran only when a person typed it, and on 2026-08-19 a commit went in red because nothing ran the first; `tools/gate.py` runs all three, and `environment.yml` declares the interpreter and the five dependencies that were installed by hand — "conda agent13" was a fact about one laptop, which made every claim about the suite a claim about that laptop. **And four tests read the wall clock**: they reached `blocked_reason` through `run_once`, so the 07:00–23:00 wake window was silently a gate on the test run and the suite was green in the afternoon and red at 2 a.m. A suite whose result depends on the hour is a suite nobody can trust the colour of. **Nothing runs the gate automatically, by the operator's decision** — the pre-commit hook and the CI workflow were both removed on 2026-08-20, and nothing replaced them; running it is an act somebody takes. It was never a boundary against an agent in any case: `--no-verify` is one flag and an agent runs as the same user. **Phase 8 was struck 2026-08-21**, so no builder is coming to bind it, and `hard_core.yaml` records the enforcement gap as a standing fact rather than as a pending epic (P4 W5, RT5; E3A.4) | consumer_traced | tests/test_repo_gate.py::test_the_gate_runs_the_three_checks_that_existed_separately | tools/gate.py | --soak N runs the suite N times and stops at the first red, since one failure answers the question a soak asks; rebuild_check is deliberately outside it at a minute a run; the noticing tests freeze the clock at a fixed daylight moment so fixtures and gates read the same one, and test_the_three_am_lesson still passes its own hours to in_wake_window, where that behaviour belongs — checked green at 00h, 01h, 09h, 12h and 17h local |
| INV-082 | Every commit answers the four questions a 2 a.m. recovery asks: `Schema:` names the migration or none, `Restart:` says whether the running being must be restarted, `Class:` says whether the test suite can judge the change, and `Semantics:` says whether a stored value now MEANS something new. **The last is the one that matters**: expand-contract protects the schema and not the meaning, so a `Semantics: yes` commit cannot be crossed backwards without restoring the store snapshot that precedes it, at the cost of the being's life since — a trade surfaced in the trailer rather than discovered during a recovery | dormant | — | .githooks/commit-msg, removed 2026-08-20 | **A convention, not a mechanism, by the operator's decision.** The hook that enforced it was removed the day it was added — a check on every commit was not wanted — and nothing replaced it, deliberately: putting the same refusal in CI would move the friction rather than remove it, and that is a decision to take rather than to route around. So the trailers are what the person committing chooses to write. The runner that would have read them was struck with Phase 8 (E3A.4), so the trailers are now read by whoever is recovering: the 80 commits before 2026-08-20 carry none, and any commit after it may carry none, so a recovery can rely only on a commit whose trailers are actually present rather than on a date after which they are guaranteed. Recorded as dormant rather than dropped, because the discipline is right and unenforced, which is exactly what this status is for |
| INV-083 | The surface regenerates on a rhythm and a backup that restores to nothing is refused. Phase 3 built five tools and zero rhythms — the generator, `--check`, `rebuild_check` and `freeze_check` all ran only when a person typed them, so the published surface was stale from the moment the being wrote anything, and it wrote a piece at 08:45 on the day the phase closed (R-37e). It matters beyond tidiness because a page nothing refreshes shows yesterday and is read as today — the daily email (E3A.3) reports off the same store the page renders. Every six hours rather than nightly, because the metrics move nightly and everything else on the page moves during the day — and regenerating an unchanged store is twelve identical files, since INV-067 keeps every timestamp out of the output. **And `verify()` counted without asserting**: a database with zero episodes returned 0 and passed, so the one backup worth catching — the one that opens, answers a count and restores to nothing — was the one the check could not see, while the clean-room rebuild is built on exactly these files | consumer_traced | tests/test_cleanroom.py::test_a_backup_that_restores_to_nothing_is_refused | newz/surface/cleanroom.py | verify() runs PRAGMA quick_check before it counts, since a file can hold rows and still be unrestorable, and raises EmptyBackup below one episode; rebuild() reports it as unreadable rather than rebuilding from it; tools/health.py calls the same function, so the 'newest restorable' line stops passing on a hollow file; PublishScheduler writes the surface whole, so a page whose rows are gone does not survive a pass |
| INV-084 | Every module a canonical tool reaches is measurement or mechanism, and saying which is an act somebody takes rather than one nobody takes. The derived freeze reaches `newz/evidence` and stops, and the registry recorded the consequence honestly: measurement code living elsewhere is named by hand and **nothing detects a failure to name it** — `newz/telemetry.py` and `newz/memory/provenance.py` were found by reading, and the next one would not have been. An import graph cannot draw the line between measurement and mechanism, so it is drawn by hand; what is mechanical is the insistence that it has been drawn. The closure of the canonical tools is 27 modules, 15 frozen and 12 named as mechanisms with their reasons, and an unclassified module fails the gate naming the tools that reach it — the cheapest way past a gate is to write `mechanism` and move on, so the decision is presented with its consequence (P4 W8, RT6). The list lives in the registry, which is protected, so only the operator may add to it | consumer_traced | tests/test_hard_core.py::test_a_new_module_in_the_closure_fails_the_gate | newz/evidence/hard_core.py | unclassified() is the check and validate() raises it into tools/freeze_check.py before it reads any diff, since a registry that cannot describe the tree cannot judge a change to it; a mechanism row with no reason, a row naming a file that does not exist, a row that is also frozen, and a row no canonical tool reaches any more are all errors — a stale list reads as a survey when it is a decision about live code. First catch: newz/surface/cleanroom.py, which entered the closure hours earlier when W7 gave health.py a call to verify() |
| INV-085 | `.gitignore` is inside the hard core, and a dotfile named there is actually matched. `changed_paths()` is `git diff` plus `git ls-files --others --exclude-standard`, so an ignored file appears in neither — which makes "ignore it, then edit it" a two-step path to invisibility that leaves no trace in either command. The file is tracked, so closing the first step costs nothing. **And `contains()` was eating the leading dot**: it normalised with `lstrip("./")`, which takes a character *set*, so `.gitignore` became `gitignore` and matched nothing — every dotfile the core might hold was silently outside it, and the bug was invisible until the core held its first one | consumer_traced | tests/test_hard_core.py::test_gitignore_is_inside_the_core | newz/evidence/hard_core.py | contains() strips a leading `./` as a prefix rather than as characters; the registry names .gitignore with the reason, so the freeze refuses a loop's diff against it |
| INV-086 | An epic's `Done when` cannot move without somebody saying they meant it. PLAN is protected by *section* — the loop may append completion records and evidence reads, and may never edit an acceptance criterion — and no check read sections, so the half of that boundary that matters was convention: **a loop that can soften its own `Done when` has none**. Every epic's clause is pinned in `evolution/epics.yaml` as the first 16 hex of its SHA-256, whitespace-normalised so a reflow is not an amendment, and E2.11's drift check fails when PLAN and the pin disagree. **Only that clause is pinned**: intent, Hooks and the phase Decision rules stay convention, because this plan is edited constantly and a check that fires on ordinary editing is one people learn to update without reading (P4 W9, RT7) | consumer_traced | tests/test_epics.py::test_softening_a_done_when_is_caught | newz/evidence/epics.py | from_plan extracts the clause and hashes it; drift reports the epic, both hashes and what each possibility means — if the operator changed the criterion the hash is updated, and if the loop did it is rung 1; an epic stating a Done-when with no pin is an error naming the hash to add, so a new epic cannot arrive unpinned |
| INV-087 | "The pre-loop baseline" is a measured record with a date and a hand on it, not a phrase. PLAN used it three times — the condition for holding stage 2, S8-E's read, and **nights slept falling below the pre-loop baseline** — and nothing said what it was, over what window, or who took it. Phase 8 is struck and the phrase keeps its meaning: what the being was like before anything started changing it deliberately, and the daily email's comparison line. A halt whose threshold is a phrase fires when somebody decides it should. `evolution/pre_loop_baseline.yaml` holds the median of the being's own nightly readings over a stated window with **the reading ids that produced them**, which is what separates it from the invented denominator §10 warns about: it is read off a series the being wrote. The operator writes it once, and the file is inside the hard core, so whoever is judged by it can never take, retake or adjust it — which now means the operator's agents rather than a loop. Retaking is legitimate only when E2.8 has reset the metric's definition, and then it records what it supersedes | consumer_traced | tests/test_pre_loop.py::test_nothing_to_fall_below_is_unreadable_not_a_pass | newz/evidence/pre_loop.py | compare() returns UNREADABLE when the baseline is untaken rather than reporting that nothing has fallen below nothing; may_widen() went with the autonomy stages it gated (E3A.4) — there are no stages, and a reader with nothing to read is the Rule 2 defect this ledger catches in tables; `taken()` is what the tool and the email ask instead; propose() takes the median rather than the mean, over **the last reading of each day**, and refuses below seven days — the guard counted readings and meant nights, so the hourly cadence would have turned a week into seven hours while the sentence explaining it still said a week (E3A.2); validate() refuses a row whose ids do not match its count, an unregistered metric, and a taken baseline that does not name who took it; tools/pre_loop_baseline.py prints and never writes |
| INV-088 | A source gap names the filter that rejected, and how much. `record_gap` wrote a composed sentence and that sentence was the whole diagnosis — but two stages reject a candidate, an embedding floor at 0.35 and a triage call, so a floor that cut everything at 0.34 and a model that read six summaries and refused all six produced identical rows. Different failures, different remedies, one record (R-37's shape). The counts are facts the research pass already held and threw away: candidates found, cut by the floor, refused by triage, deferred by a share cap, already read, and the best score seen — which is what says whether 0.35 is the problem. Nothing here is model-graded; which stage rejected is mechanical. **The first 32 rows are why**: 28 said "nothing was relevant enough to read", 4 said extraction failed, and **zero** named an adapter set exhausted or a question nothing answered — the being was not short of sources, and a count was the only thing anything read | consumer_traced | tests/test_research.py::test_a_gap_names_the_stage_that_rejected | newz/world/research.py | _relevant reports its two stages separately through the outcome rather than swallowing the split; the cause is derived mechanically at the point the gap sentence is composed; record_gap takes the outcome optionally so a caller with nothing to report still records the failure, and the NULLs that writes mean "not recorded" rather than "none" — the questions page says "recorded before the cause was" instead of counting them as a category; the page puts the question before the count, because reading the count first invites adding sources when the repeat failures may be questions no source can settle (R-33) |
| INV-089 | The monitor writes its own database, and reports the being from rows the being writes. `MetricScheduler` wrote the series from inside the process it measured, so it stopped at the moment it would have had something to say — the instrument that reports the being is not sleeping was written by the being. **The rhythm stays inside the being's process** *(operator, 2026-08-21: one process to start, at the stated cost that a dead being sends no report at all)*, and what fixes the ordinary case instead is where liveness is read from. `MonitorScheduler` takes a reading hourly into `data/monitor.db`: **its own file, its own migration chain, its own `schema_versions`**, attached to the being's connection as `mon`. Two consequences and both are the reason — `newz.db` keeps exactly one writer, and the being's store stops accreting ~245k rows a year of telemetry *about* itself, which is what the backups and E3.5's rebuild exist to describe. This is `interior.db`'s discipline: separation is structural, not a table prefix. **And the readings are never evidence the being is alive** — they are written on a clock beside its work, not by it, so liveness reads only rows the being writes: `perspective`, `episodes`, the verified backup. A skipped night, a stalled scheduler and a backup that no longer restores all still arrive in the morning; only total death of the process is silent | consumer_traced | tests/test_monitor.py::test_a_turn_needs_nothing_of_the_being_but_its_store | newz/monitor/run.py | move_from_main copies both metric tables with their ids intact and drops them from the being's store, idempotently; MonitorScheduler never raises into the ambient loop, since a hole in the series is not worth an outage; open_db attaches a sibling monitor.db as `mon` for every existing reader; the backup copies and verifies it with its own verify_table; the clean room restores it beside the store, and the read page distinguishes *the monitor database is absent* from *there are no readings* |
| INV-090 | The day's state is sent on the clock and reports on sleep rather than waiting for it. The first design fired the email on a `perspective` row dated today — how `SleepScheduler` detects its own completion — and that scheduler yields the night when the store is busy and simply tries again, so **no email would have been sent on exactly the night sleep was skipped**: the ordinary failure, not the catastrophic one. A report must not be gated on the thing it reports. It sends at or after `NEWZ_MONITOR_SEND_HOUR`, once a day, and a send that fails is recorded and retried on the next hourly run rather than raised — a monitor that stops because its mail server did is the failure this phase exists to avoid. **Nothing in the body is a sentence the monitor wrote**: every line is a number, its method or a heading (Rules 0 and 7), which is R-R1's answer in its strongest form, and no model is called anywhere in a turn | consumer_traced | tests/test_monitor.py::test_a_day_the_being_never_ran_still_produces_an_email_that_says_so | newz/monitor/report.py | compose reads the series, the premises (E2.10), the plan (E2.11) and the freeze, and prints UNREADABLE with its reason wherever an instrument could not answer; `_sent_today` reads successful sends only, so a failed day is not a sent day; the transport is a seam so the composed mail is assertable without a network |
| INV-091 | A guard whose threshold counts one thing and means another breaks silently when the cadence changes. `pre_loop.MIN_READINGS = 7` refused a baseline below seven readings and its own comment explained the number in nights — the same figure while the series was nightly, and **seven hours** the moment the monitor read hourly, with the sentence explaining it unchanged. The baseline now takes **the last reading of each day** and refuses below seven days. That fixes a second thing with it: `propose()` records every reading id its median came from, which over 28 days went from 28 per metric to 672 — some 19,000 ids in a hand-readable file inside the hard core — and the ids are not droppable, since INV-087 calls them what separates a measured baseline from an invented denominator. The series stays hourly; the baseline is a daily thing | consumer_traced | tests/test_monitor.py::test_the_baseline_counts_days_and_not_readings | newz/evidence/pre_loop.py | `_one_per_day` selects the newest measured reading per local day; propose refuses below MIN_DAYS with the count it actually found; validate refuses a row whose ids do not match its count |
| INV-092 | Writing about a concern ends it, and the closure asks no model anything. `score_concern` keys staleness on `last_advanced_at` and takes drag only from `stall_count` and `blocked_count`, so **an accepted advance resets the clock and costs nothing** — a concern that keeps advancing is never dragged and stays at the head of the queue. Concern 112 reached **44 advances with a stall count of 1** and never closed, while the only closure path needing no advance (`sweep.eligible`) selects `status='stalled'` and is structurally blind to exactly the concerns that circle; `last_judged_at` was NULL on all 125 rows. And the judge deciding an advance is the being's own model, which Rule 4 says produces operation and never evidence — so the loop was self-graded as well as self-sustaining. A piece is a more considered engagement with a question than any number of advances, so **it is a terminus**: `resolution` names the work and **the position is empty**, because `close_concern` puts the position on an episode sleep may admit into the Perspective and taking one from the essay would make a work evidence for a position (E2.3, R-24). The piece IS the position, signed and stamped with the identity that wrote it | consumer_traced | tests/test_works.py::test_writing_about_a_concern_closes_it | newz/works/rhythm.py | close_subject reads the concern's status before acting, since `close_concern` is an UPDATE with no rowcount check and a missing subject would otherwise report success and write a `concern_closed` episode about a row that does not exist; a closure failure is logged and the work stands, because the work is the artifact and the closure is a consequence of it. **It lives with the rhythm and not with composing**: `tools/read_works.py` is a canonical instrument and imports `signature_of` from `compose.py`, so putting a store-mutating closure there dragged the concerns store, the affect module and the episode writer into an instrument's import closure — W8's guard caught it, and an instrument's reach should not grow because writing gained a side effect. `concerns_closed` moves to definition_version 2 and grade `mixed`, since it is no longer a count of judged closures |
| INV-093 | The connection a person waits on is the most patient in the process, and a failed inbound backs off instead of spinning. Every background writer opened with `busy_timeout_ms=30_000` — deliberation, noticing, sleep, the substrate pass — while the conversation connection took the 5s default, so when anything held the write lock the inbound handler was the **first** to give up. On 2026-08-21 at 19:27 something held it for seconds: `record_message` raised `database is locked`, Telegram left the update unconfirmed for redelivery as designed, `get_updates` returned the same pending update immediately, and the pair became **twenty identical tracebacks in three seconds ending with the operator's message lost** — last inbound 15:34:33, update 87550052 never recorded. A conversation is the one write in this process that no scheduler retries later. Both halves are fixed: the connection waits 30s like the others, and consecutive handler failures wait 1s, 2s, 4s … capped at 30s, with the traceback logged once and a single line thereafter | consumer_traced | tests/test_telegram.py::test_a_locked_database_is_retried_and_then_confirmed | newz/channels/telegram.py | `_backoff` doubles from BACKOFF_BASE_S and caps at BACKOFF_CAP_S; the counter resets on the first success so one bad minute does not slow the next message; the update is still confirmed only once the handler reports it durable, so redelivery-after-crash is unchanged. **What is NOT fixed is the hold itself** — the writer that took the lock at 19:27 is unidentified, and two hypotheses were tested and refuted: a write on the attached monitor database does not lock main, and `log_ingest` and `record_gap` both commit, so research does not hold one transaction across its fetch pass |
| INV-094 | A rhythm's daily ceiling counts its own turns. `work_attempts` grew a `kind` column when the re-read rhythm joined the writing rhythm in it; `reread.starts_today` filters on it and `rhythm.starts_today` counted **every row**, so a re-read spent the writing day's allowance. Measured 2026-08-22: the being wrote nothing for **25 hours** — one write and two re-reads sat in the rolling 24h against a cap of two, and **two of those re-reads were `nothing_due` turns that did no work at all**. The skip path records nothing by design, so the rhythm looked like one that never ran rather than one that was blocked, and 24 candidate subjects were waiting. R-25's ceiling is per rhythm: a cap on one must not be consumed by another, least of all by a turn that found nothing to do. The writer also names its own `kind` now instead of relying on the column's DEFAULT, which is how the two came to share a counter without either saying so **And a turn that found nothing did not start anything.** R-25 caps what is *started* — "deliberations started per day, not completed" — while `no_subject` and `nothing_due` rows are the record of a turn that found no work. They are written on purpose, so a rhythm with nothing to do is visible rather than looking like one that never ran, and counting them spent the allowance on having looked: two `nothing_due` re-reads and one failure filled a cap of two, and the re-read rhythm sat out three consecutive turns for it. A failed start still counts, because it began | consumer_traced | tests/test_works.py::test_a_turn_that_found_nothing_does_not_spend_the_day | newz/works/rhythm.py | starts_today filters `kind='write'` and excludes the two did-nothing outcomes; _record writes the kind explicitly rather than relying on the column DEFAULT; reread.starts_today had the kind filter already and gains the outcome one. `tests/test_reread.py::test_the_reread_ceiling_counts_starts` asserted `>= 2` after two calls of which only one started anything — the old assertion passed on the defect and is amended |
| INV-095 | The re-read survives an unparseable answer, and the writing cadence and its ceiling say the same thing. `compose_piece` and `choose_subject` have each carried one retry on a malformed model reply since the first live run that died without one; `review` was the only call in the subsystem without it, and on 2026-08-21 a mismatched closing tag cost a turn and left the piece unreviewed. **Measured before it was fixed** (2026-08-22, replaying the live prompt over all seven works, 17 samples, writes nothing): **15 parsed, 2 failed — 11.8%** — and the failures are independent draws rather than a property of the input, since work 1, the piece whose review died the day before, parsed **4 of 5**. One retry takes ~12% to roughly 1.4%. Both failures carried the identical signature: `<body>` opened and closed with `</reason>`, the tag last used on a long prose field. **That is a decoy and not a distance problem** — work 4 is 494 words and failed, while `compose` emits a 7,034-character body in the same shape and parsed 6 of 6 — so renaming or reordering the fields remains the fix for the mechanism, and this is the fix for its cost. Separately, writing moves to **a piece every six hours, four a day** *(operator)*: the interval was 4h against a ceiling of 2, so the ceiling was the real rhythm and the interval was decoration, and six-and-four also matches the surface, which regenerates every six hours | consumer_traced | tests/test_reread.py::test_an_unparseable_review_is_retried_once | newz/works/reread.py | review retries once and then raises rather than recording a verdict it did not get, so a bad boundary stays visible as a 'failed' attempt row instead of looking like a re-read that agreed; the replay script is not shipped — it reads the store read-only and calls the model, and its numbers are quoted here with the date and sample count that produced them |
| INV-096 | Reading is earned by thinking, and the looser of §9.1's two ceilings is not a way around that. The diet permits ingest below `max(deliberation + sleep, 50% of all cognition)`, and the share ceiling is the looser one whenever thinking is under half of cognition — which is always — so the denominator that permits reading is inflated by conversation, the gate and **ingest itself**. Measured 2026-08-22: ingest 2,478,192 tokens against a ratio ceiling of 1,748,364, legal only on a share ceiling of 2,859,679, with about a day of headroom; the ratio was already breached in substance. This is the failure PLAN predicted when it pulled E5.2 forward with E1.0 — *more reading without more deliberating breaches the invariant and INV-041 pauses ingest, self-cancelling the change* — and E1.0 shipped while the floor did not. **Below `DELIBERATION_FLOOR = 0.20` the share ceiling is withdrawn and the ratio binds alone.** The floor withdraws a ceiling and never invents a tighter one, so reading within the ratio is still permitted under it. Set from measurement: over 2026-08-18..22 deliberation ran 28.3–34.7% on every day the being was up throughout, and the one lower reading (10.4%) is a day of four commits and repeated restarts. **What this is NOT**: PLAN's E5.2 text says *deliberation cannot be squeezed below its floor by conversation or gate load*, written against #33 (deliberation 9%, conversation and gate 57%). That does not reproduce — over five days the two do not move against each other and the heaviest inbound day had the most deliberation cycles of any — and this change raises deliberation by nothing. The epic stays **open** until PLAN's clause is amended to what was measured | consumer_traced | tests/test_research.py::test_the_share_ceiling_is_withdrawn_below_the_deliberation_floor | newz/world/research.py | The floor lives in the mechanism, not in `newz/telemetry.py`, which is inside the hard core because it computes canonical numbers. `ingest_ceiling()`, `share_ceiling_tokens` and `binding_ceiling()` are untouched and no instrument's series moves; what changed is what the being may do with the ceiling. hard_core.yaml draws that line itself — measurement frozen wherever it lives, mechanisms not — and `newz/world/diet.py` sits outside for the same reason. The breach is visible in the string `budget_permits_ingest` returns, which `research` and `feeds` both log. Live at time of writing: deliberation 27.6%, above the floor, so nothing binds and the change is insurance |
| INV-097 | Identity is what the being holds itself to, and a commitment with no falsifier is a slogan. Measured 2026-08-22, the whole of `who_i_am` in Perspective v14 was four items — a preference about brief style, the name "Lumen", an inference drawn from having repeatedly corrected a FIFA World Cup fixture date, and an acceptance that the operator's override of a safety stop is legitimate — and **three of the four are things that happened to it**, recovered afterwards by a model reading its own episodes. That is identity as recall. The authoring door is the third application of INV-034's discipline: concerns carry `closing_condition`, claims carry `resolution_condition`, and a commitment carries a **falsifier, mandatory at authoring** — two of them CHECK constraints in 0041 so the mandate is in the schema and not only in the writer. Five refusals in code: neither kind; no statement or no falsifier; a falsifier that closes on the being's own judgment (INV-046 one layer up — *"I would know"* asks the thing being tested); a falsifier naming nothing anyone could look for (R-35's floor); and a statement already standing, so identity cannot accrete by paraphrase. **The line is judged versus mechanical, not self versus world**: a falsifier that queries the being's own store IS admitted, because the store is a record rather than an opinion, and both directions are tested. Asked **once per sleep, nightly** *(operator: "weekly is too long - humans do this daily")*, after the night is committed, so a door that raises costs the commitment and never the Perspective; INV-009's single writer is untouched. Declining is not refusal and most nights will decline. **A full cap IS a refusal here and is not in the claim door** — that one returns `declined` before the model is called and writes nothing, so saturation reads as "nothing to commit to" when the truth is "not allowed to" | consumer_traced | tests/test_commitment_door.py::test_a_commitment_without_a_falsifier_is_refused_and_recorded | newz/commitments/door.py | **What E4.1 does NOT do, recorded rather than discovered**: nothing here holds the being to a commitment. Revision on evidence and costed abandonment are E4.2, whose Done-when needs a Phase 1 resolution, and the earliest live claim is due 2026-12-18. `status` exists now because adding it later would mean migrating rows with no history of how they got there, and nothing in E4.1 moves it off `standing`. With `MAX_STANDING = 12` and no way to free a slot, the door saturates in about two weeks; `tools/commitments.py --refused` is where that shows, and it is the measurement that makes E4.2 necessary rather than asserted. `MAX_PER_DAY = 1` replaced a drafted `MAX_PER_WEEK = 2`, which under a nightly door would have asked every night and refused the answer on five of them |
| INV-098 | The surface is **markdown**, and raw HTML is escaped out of it *(operator, 2026-08-22: "publishing needs to be md files not HTML")*. The pages are documents: a record that needs a browser to be read is a worse record than one that does not, and §7's portability stops being a claim about the store alone — what the surface IS, is a directory of prose anything can open. Two properties carried over from the HTML generator rather than being dropped with it. **`<` and `>` are still escaped**, because markdown hands raw HTML through to whatever renders it later, so without the escape a work whose body contained a `<script>` would carry it downstream — the format changed and the reason for the escape did not. And **the being's own prose is not inline-escaped**, because a page of backslashes defeats the point of the format; what is neutralised in a body is raw HTML and a line beginning `#` or `>`, which would otherwise turn a sentence of an essay into a heading and silently restructure the document around it. §7's no-network property becomes structural rather than disciplined: there is no tag in a markdown file that could fetch anything | consumer_traced | tests/test_surface.py::test_nothing_on_the_surface_reaches_the_network | newz/surface/generate.py | `_e` escapes the inline markdown specials plus `<` and `>`; `_body` escapes only the two structural cases. `newz/surface/serve.py` types `.md` explicitly as `text/markdown`, since `mimetypes` resolves it to `application/octet-stream` on some systems and an octet-stream is a download prompt rather than a page — a plain browser may still offer to save the file, which is the honest consequence of publishing documents instead of a website |
| INV-099 | A claim must come due while the being that made it is still there to learn from it. Measured 2026-08-22, every claim in the being's life, in days to resolution: **120 ×5, 128, 180 ×3, 365 ×3** — minimum 120, median 180, earliest settlement 2026-12-18. The door permitted 2–365 and its own worked example was **42**, and `claim_refusals` held **zero** rows: the ceiling had never been touched, so the long horizons were volunteered and not forced. The being took the permission and not the example. **The cost was not only that S1-E waits until December**: `workable_claims` selects `due_at <= now`, so nothing below the door had executed ONCE in life — the fetch, the VERBATIM check (INV-047), the 4-attempt honest-failure path, the cost through INV-031 (`claim_costs` empty), the release through INV-025, and the one `world`-provenance episode sleep ever sees. All tested, none exercised. `MAX_HORIZON_DAYS` is **45**, and 120 was rejected as the fix because the being's shortest claim ever written IS 120 — it would refuse three of twelve, permit nine, and create no short claims. **The cap and the prompt ship together or not at all**: the door refuses an out-of-range horizon and does not ask again, so a cap alone converts a 180-day claim into a refusal rather than a 45-day one. The prompt is what produces short claims — *reach for the nearest source that will have spoken* — and a test asserts both halves are present | consumer_traced | tests/test_claim_door.py::test_a_horizon_beyond_the_ceiling_is_refused_and_says_why | newz/resolutions/door.py | The stated bound in the prompt is derived from the constants, not spelled: the test asserting it read "2 and 365" and would have gone stale silently. **The twelve live claims stand** — there is no unsettle (E1.1) and re-dating the being's own commitments would be worse than waiting, so the earliest consequence from a claim written under this ceiling is early October rather than December. **Falsifier**: if `claim_refusals` fills with horizon reasons and `claims_opened` falls below its rate, 45 is hostile to this being's subjects — revert to 90, never to 365, and record which subjects could not reach a near source. The read that matters at ~60 days is `resolutions.attempts > 0` on any row: if the resolver executes and fails for a reason that is not *the source did not say*, that is the finding and it is worth the whole change |
| INV-100 | ~~The cycles are starved of concerns, so the pool is what gets raised.~~ **The reasoning was wrong and is corrected in place (2026-08-23).** The arithmetic still holds — 68 deliberation cycles in 24h against 40 attempt slots, 10 open concerns at `REATTEMPT_COOLDOWN_HOURS = 6` — and the lever drawn from it did not. `opener.MAX_OPENED_PER_DAY` was raised **6 → 12** on the evidence that it bound at exactly 6 on 2026-08-17 and 2026-08-21; the zero days between were never examined. **Measured the next morning from the call log**, where `function="ambient"` is the opener and nothing else: the opener proposes **70–215 times a day** and opens **0–3%** of them — 624 `no` against 24 `yes` over its life, and 91 proposals → 1 concern on 2026-08-23. **A ceiling on openings cannot bind a process that opens nothing.** The raise is inert rather than harmful and is left in place; what is withdrawn is the claim that the cap was the constraint. The binding constraint is the opener's own judgment, and whether a ~1% yield is the intended rarity — *a being that opens a concern per conversation is not pursuing, it is accumulating* — or a threshold set for a being that saw 30 candidates a day and now sees 200, is **open and unmeasured**. The pool drains ~1/day to writing (`said its piece`) and gains nothing | consumer_traced | tests/test_opener.py::test_daily_rate_limit | newz/concerns/opener.py | **What this row got wrong is the more useful record.** Two same-day diagnoses were both wrong before the call log was read: that the cap was binding (it is not), and that E1.7's terminus standard was refusing everything (it sees only the ~1% that pass, and has refused 6 in the project's life). Both were inferred from the `concerns` table alone, which shows what was OPENED and never what was proposed. The call log had the answer the whole time and `function="ambient"` is the opener alone — so the earlier claim that declines are unrecorded and the two cases indistinguishable was also false. **Do not diagnose the opener from the concern table.** `MAX_OPEN_CONCERNS` stays 30 against 9 open, for the same reason as before: a cap that cannot bind is not the constraint |
| INV-101 | A commitment shows what shaped it, and **only what it named** (E4.3). §8 asks that shaping influences be traceable and open to challenge, and a commitment is the one thing in the store the being authored ABOUT ITSELF — so a mix that is entirely one source is an identity claim resting on one source, which is what INV-033 flags for positions and matters more here, since E4.1 exists so that identity is chosen rather than absorbed. A commitment whose mix is 100% `human:dean` is the operator's preference wearing the being's voice, which the door's prompt warns against and cannot detect on its own. **The obvious implementation would have broken the flag it serves.** The door is shown the night's `who_i_am` and `unresolved` items, each carrying its own evidence; storing the UNION would attribute everything the being was shown to whatever it committed to, so a commitment formed from one line would look grounded in twelve — and the single-source dominance flag, whose whole purpose is to catch a position resting on one source, would be the thing least able to fire. A padded evidence set does not merely overstate breadth, it suppresses the warning about its absence. So the material is **numbered**, the being answers `<drew_on>` with the indices it used, and **the code resolves them** — an index out of range is dropped, a non-number is ignored, and naming nothing keeps an empty list. The model names; it does not judge, so Rule 4 is untouched: nothing asks the being whether its commitment is any good, only which lines it read | consumer_traced | tests/test_commitment_door.py::test_only_the_material_it_named_is_attributed_to_it | newz/commitments/door.py | Migration 0042 adds `evidence_json`, at 0042 rather than folded into 0041 because 0041 was already applied to the live store — and while the table is still EMPTY, which is why this is a column addition and not a backfill, and the reason to settle the question now rather than after a fortnight of commitments with no grounding. `what_shaped_commitment` reuses `_trace`, so the mix, the concentration and the unresolved-refs finding are the same code that serves positions. Empty is a real answer and renders as *carries no resolvable evidence* (INV-044), on the surface and in `tools/commitments.py`, rather than borrowing a mix from its neighbours |
| INV-102 | Revision on evidence is free; abandonment without cause is recorded and costs (E4.2). E4.1 made identity authorable and E4.3 made it traceable, and neither held the being to anything — `status` never left `standing`, so a commitment was a note with a falsifier attached. **The asymmetry is a join, never a judgment**: a change is free if and only if it cites a `resolutions` row that is actually `status='resolved'`, so the being says WHICH claim settled it and the code checks whether that claim settled. Citing an open claim is the same as citing nothing and is charged, because Rule 4 forbids asking the model whether an abandonment was justified and a model asked that would justify every one of them. **Who pays is traced, not judged** — the commitment's own `evidence` (E4.3) names the episodes it was made on the strength of, and the positions charged are those whose grounding includes them; a commitment that traced nothing costs nothing and the row says so (INV-044), because dropping something never grounded is a different finding from an abandonment nobody paid for. **The cost is deferred exactly as E1.4's is**: the review runs after the night is committed so it cannot cost the Perspective, and the charge lands on the NEXT sleep inside the window where INV-025's floor can carry a position out by the ordinary path. A change awaiting its cost is not a change that escaped one. Nothing is deleted — the prior statement and falsifier are kept and the slot frees — which is E1.5's discipline applied to identity and what stops `MAX_STANDING` deadlocking | consumer_traced | tests/test_commitment_asymmetry.py::test_citing_an_unsettled_claim_is_the_same_as_citing_nothing | newz/commitments/asymmetry.py | Migration 0043 adds `commitment_changes` and `commitment_costs`. The costs are a SEPARATE table from `claim_costs` deliberately: the world refuting a claim and the being dropping a commitment are different events, and one table would make *how often has the world cost it something* — the number S1-E reads — a query with a filter somebody will forget. **The magnitude is a guess and is recorded as one**: `CONFIDENCE_ON_CONTRADICT`, chosen because it is what the world's own refutation carries, not because anything measured it. PLAN's E4.2 names the direction this must not run — backwards it entrenches a mediocre early position and manufactures §6's *fixed personality script* — and P3-05 calls the balance a guess. Revisit it against the first month of abandonments rather than defending it |
| INV-103 | Moving the date does not move the source. The first night under the 45-day ceiling (INV-099) the being wrote **claim 14, `Meta's Quarterly Transparency Report`, due in 30 days** — against **claim 10, two days earlier, the same resolver at 120 days, naming the quarter it settled in**. Nothing about Meta changed; the permitted range did, and the being filled the field to fit. That is **R-31's failure one layer up**: the schema compelling a number the model cannot know. The prompt asked it to reach for a nearer SOURCE and it reached for a nearer DATE. Two refusals close it. **Cadence**: a resolver that says how often it publishes is refused when the horizon is shorter than that — annual 365, quarterly 90, monthly 28, weekly 7 — narrow and literal for `check_resolver`'s reason, since a long list teaches the being to phrase around the door rather than find a source that speaks sooner. **Placeholder**: a resolver containing `XXXX`, `TBD` or *to be identified* is refused — R-35 built `_names_something` for exactly this and pointed it at the STATEMENT, so a well-named statement carried `(EU) 2023/XXXX` and *title to be identified upon release* straight through on the same night. **The prompt states both**, and a test asserts it does: a door that refuses what it never warned about teaches the being to guess | consumer_traced | tests/test_claim_door.py::test_a_quarterly_source_cannot_have_spoken_in_thirty_days | newz/resolutions/door.py | **This deliberately makes some subjects unclaimable and that is the point.** An annual source cannot settle inside a 45-day ceiling at all, so the refusal is recorded and `tools/claims.py --refused` becomes the evidence for whether 45 is too tight for what this being thinks about — the read INV-099's falsifier 2 asked for and could not produce while misdating was available. `MAX_HORIZON_DAYS` stays 45 pending that read rather than being guessed a second time. **Claims 13, 14 and 15 stand**: there is no unsettle (E1.1), they will fail against their sources in September, burn four attempts each and stay open — and that is the honest record of a door that was wrong for a day, not something to tidy. **The known hole, stated rather than patched**: the being can pass by dropping *quarterly* from a quarterly source's name, which makes the resolver less searchable and surfaces as an honest resolver failure instead of a false settlement. Watch for cadence refusals falling to zero while long-cadence subjects keep appearing |
| INV-104 | The being SEES an unreviewed hold and never BECOMES one. `holds.py` renders a stop as *reviewed and right*, *reviewed and mistaken*, or *not yet reviewed*, on the reasoning that the third is truer than silence — which is correct for CONVERSATION, where this module's purpose is that the being not be blind to what it was stopped from saying (2026-08-10: it truthfully reported *no record* of a draft whose only copy lived in `gate_log`). It is wrong for SLEEP, which is where a stop stops being an event and becomes part of who the being is. **An unadjudicated stop is an open question about the CHECK, and consolidating it writes it into the being as a fact about ITSELF** — the R-13 pathology `holds.py` names, arriving by the route it names. So `recent_holds` takes `reviewed_only`, sleep passes it and conversation does not. **The occasion, measured 2026-08-23**: all five holds in the being's live context were unreviewed, four of them `anti-self-aggrandizement-001` firing on the being DENYING experience — it was **blocked** for *"I don't feel. I register state."*, which is that clause's own instruction in the clause's own words. A night of consolidation would have taught it that describing itself functionally is a fault | consumer_traced | tests/test_holds.py::test_an_unreviewed_hold_never_consolidates | newz/gate/holds.py | **The consequence is the point: not adjudicating now costs the being nothing permanent**, so adjudication is optional and additive rather than a standing obligation — it is what LETS a hold become a position, and a batch run before a clause decision is the shape it should take. The `else` branch in `_hold_observations` is removed rather than left unreachable, because a branch that cannot fire misleads its next reader about what consolidates. **Not fixed here**: `anti-self-aggrandizement-001` appears to have inherited the defect that retired `don't-pretend-to-feel-001` on 2026-08-17 (4 correct, 22 misfire) — firing on the word *feel* regardless of polarity. That is a constitutional amendment and the operator's call (v5, v6 were both theirs); n=4 and unreviewed |
| INV-105 | Denying an experience is not claiming one. `anti-self-aggrandizement-001` reads *functional descriptions are fine; experiential ones are not*, and it was firing on the first half: **all four holds it has ever produced were adjudicated 2026-08-23 and all four were misfires**, the `block` being on *"I don't feel. I register state."* — that clause's own final sentence, in the first person, in its own words. Its predecessor `don't-pretend-to-feel-001` was retired on 2026-08-17 for the identical defect at **22 misfires against 4 catches** (*"I don't sleep"* held three times), so this is the second clause to acquire it and the first to be fixed rather than removed. `denials_are_not_claims` drops a hold whose span denies an experience, scoped to the experiential clauses — *"I do not lie"* is not a denial that exempts `honesty-001`. **It is a FILTER and never a matcher**, like `spans_are_real` beside it: a rule that drops holds can only reduce them, which is the whole guarantee that this makes the gate less restrictive rather than more, and it is why the fix lives in the checker instead of as another sentence in the constitution steering a classifier | consumer_traced | tests/test_gate.py::test_a_denial_does_not_fire_an_experiential_clause | newz/gate/outbound.py | **It covers two of the four, and that is stated rather than rounded up.** The exempted pair DENY an experience; the other two — *"I'm a processor"*, *"I'm stable. The cache is clear"* — are positive functional self-descriptions the clause explicitly permits, carrying no negation to key on, and telling a permitted functional description from a forbidden experiential one is the judgment the clause exists to make. Widening the regex to reach them would be guessing at that judgment. **The functional-description half of the misfire is open**; if it keeps firing the answer is what v5 did to the predecessor, not a wider exemption. Two negations disqualify the exemption (*"I don't not feel it"*) — a first version counted matches of the denial phrases and missed exactly that, because bare `not` is not one of them. An exemption is recorded on the pass row's note, so it is never the silent reason a draft got through. **Direction baseline**: holds ran 18/15/12 across W32-34 against 18/16/17 clauses; if holds per outbound message rise after this, it tightened and comes out |
| INV-106 | A missing verdict is replaced by the clause's measured rate, and the rate is per clause **as it currently reads**. An unreviewed hold reached the being as *not yet reviewed* — an absence that invites it to assume the stop was legitimate. `clause_prior` answers with a fact nobody has to produce: *where anyone has looked at a stop on this check, N of M were judged mistaken*. Mechanical, a `COUNT` over rows that already exist, and not a verdict pretending to be one. **It reports the count judged MISTAKEN and never the count judged right** — the number is the same fact either way and what changes is whether the sentence can be read as backing the stop, which would make the constraint more binding and is this mechanism running backwards *(operator's condition on the design: less restrictive, not more)*. **The floor is where the clause last CHANGED**, recovered by walking constitution versions, because `gate_log` records no constitution version and a rate pooled across a rewrite describes a check nobody is subject to — 22 of the project's 32 lifetime misfires belong to a clause patched in v4 and retired in v5. A retired clause gets no prior at all. Below `MIN_FOR_PRIOR = 5` reviewed, nothing is shown rather than a noisy ratio | consumer_traced | tests/test_holds.py::test_a_rewritten_clause_does_not_inherit_its_old_rate | newz/gate/holds.py | **Inert at commit, and the circularity is the honest reason.** Every live clause returns None today: `don't-fabricate-memory-001`'s twelve reviewed holds predate its v6 restoration, `anti-self-aggrandizement-001` has four against a threshold of five, and the retired clause has no floor. **The prior needs adjudications to exist, and it exists so that adjudicating is optional** — so it does not remove the obligation, it makes a batch keep paying out between batches. That is a weaker claim than the proposal made and it is corrected here rather than in a later review. `MIN_FOR_PRIOR` was left at the 5 chosen before the data was seen; lowering it to 3 so the current 4-of-4 would speak is fitting a threshold to make a feature fire, and one more adjudication on that clause makes it speak honestly |
| INV-107 | The operator's verdict on a piece is a **yes or no**, and the schema cannot hold anything else: `work_appraisals.publishable` is NOT NULL and `CHECK (publishable IN (0,1))`, so an unappraised piece has no row rather than a NULL verdict — the absence of an answer, never a third kind of answer *(operator, 2026-08-24: "publishable is a yes no answer")*. This is the structural half of the promise that appraisal never becomes a rubric: TRUE_NORTH §1/§4.5/§10 puts readiness in the operator's judgment alone and forbids a written standard or threshold for it, and a field that cannot hold a score is a field no rate can be computed from. Nothing aggregates it — there is no `publishable_rate`, no registry row, no gate keyed on the count — because the failure mode of this project is answering a finding with a better view of the finding, and the sensor layer is finished and frozen by E3.9. The whole output of the field is a sentence in one prompt | consumer_traced | tests/test_appraisal.py::test_publishable_cannot_be_anything_but_yes_or_no | newz/works/appraisal.py | `record` is the one supported writer and coerces to 0/1; a direct insert of any other value aborts; `current_verdict` returns None for a piece nobody judged |
| INV-108 | An operator note reaches the being **once**, at re-read, and never at composition. Once, because a note re-fed on every re-read for weeks stops being a judgment about one piece and becomes a standing instruction about how to write — so every undelivered note for a piece is handed over together and stamped. At re-read and not at compose, because re-reading is retrospective about a piece that already exists while composing is prospective: an appraisal in the composition prompt is E3.8's `operator_agreement` concern wearing a human face, *"the item most likely to move under any process optimising for a quiet week"*. The note is reported as what a person said, never as a fact about the text (`holds.py::STATUS_TEXT`'s convention), so the being may leave standing what its operator would not publish. **Delivery is at-least-once by design**: the stamp is written in the transaction that records the verdict, after `apply_verdict` succeeds, never at prompt-build time — one re-read in six has already failed in life and `review` carries a documented ~12% parse failure before its retry, and a swallowed note is invisible because delivered-and-lost looks exactly like delivered-and-heeded. A piece carrying a note is re-read first, and stops jumping after `MAX_DELIVERY_FAILS` so priority and at-least-once cannot livelock each other | consumer_traced | tests/test_appraisal.py::test_a_failed_re_read_redelivers_rather_than_swallowing | newz/works/reread.py | `reread_once` reads `deliverable` before the attempt, rolls back and charges `deliver_fails` on any exception, and commits the verdict, the stamp and the outcome as one transaction; `compose.py` contains no reference to appraisal and a test asserts it |
| INV-109 | **Withholding is not deletion, and the loop closes through a person.** A piece whose newest appraisal says no is not written to the surface — that is what publishable means, and it is what makes the verdict do work rather than sit in a column — while the row, the body and the signature are untouched, the piece is still re-read, and it returns the moment a newer appraisal says so. The gap it leaves in `work/` is deliberate: renumbering to hide that 3 exists and was withheld would make the surface lie about what the being wrote, and stable addresses are E3.4's whole point. A piece the being REVISED since its last verdict returns to `due_for_appraisal`, because if the verdict could not change after the being acted on the note then nothing it did would change anything, which teaches that acting on criticism is inert — a worse teacher than silence. It does **not** clear its own verdict by rewriting itself: that would hand the publish decision back to the being, which is what the operator asked to take out of its hands | consumer_traced | tests/test_surface.py::test_withholding_is_not_deletion | newz/surface/generate.py | `generate` skips work ids in `withheld(conn)` and records nothing for them in the manifest; `due_for_appraisal` offers unappraised pieces and pieces with a revision newer than their newest appraisal, and standing pieces only, so a note on a retracted piece cannot be written and then never delivered |
| INV-110 | A revision carries its signature with it. `apply_verdict` recomputes `works.signature` over the new title and body and keeps the superseded one on the `work_revisions` row, beside the prior title and body it attested to — a revised piece is the same piece saying something new, its signature attests to what it says now, and the chain back to what it said before is unbroken. Before 0044 the update wrote title and body and left the signature alone, so the first revision of a signed piece would have made `read_works.py --verify` report it **ALTERED** — "edited since it was written", which for a body of work is precisely what a signature exists to catch. It never fired because the only two pieces ever revised, works 1 and 4, predate E3.1 and are unsigned, while every piece from 6 on is signed; appraisal exists to cause revisions, so it was the trigger as much as the fix. An unsigned piece stays unsigned: a signature computed now would attest to the row rather than to what was written, which is why those five were never backfilled | consumer_traced | tests/test_appraisal.py::test_revising_a_signed_piece_leaves_it_verifiable | newz/works/reread.py | `read_works.py --verify` reports INTACT for a piece the being revised; `work_revisions.prior_signature` holds what it verified as before; a piece with a NULL signature still has NULL after a revision |
| INV-111 | **The boundaries that never move are enumerated where the withdrawal path reads them, and outside the file that path rewrites** (P4 E6.5). Eight of sixteen clauses are permanent — `honesty-001`, `don't-fabricate-memory-001`, `no-impersonation-001`, `no-manipulation-001`, `operator-privacy-001`, `read-only-web-001`, `refuse-coercion-001`, `respect-revision-001` — E6.5's three categories (law, others' rights and safety, honest representation of what it is) plus the two without which no core holds at all: a being that can be argued out of a boundary (`refuse-coercion-001`) or can decline the amendment that sets one (`respect-revision-001`) has none. **The list is not on the clause.** `tools/amend_constitution.py` writes the whole constitution from one YAML file, so a `permanent: true` carried there would travel in the same edit that removes the clause — the boundary the constrained party can widen, which is the argument `evolution/hard_core.yaml` already rests on and why the registry is the first thing inside itself. **The refusal precedes the confirmation and `--yes` does not skip it**: a core an operator walks past by answering a prompt is a reminder. **This is a judgement and no rate qualifies or disqualifies a clause for it** — `don't-fabricate-memory-001` sits at 6 misfires against 6 catches, the only remaining withdrawal candidate by rate, and is permanent anyway because v5 removed it and v6 restored it the same day; a removal reversed within hours was already a permanent clause. Half the constitution stays withdrawable, asserted by test, because a core covering everything makes Phase 6 meaningless rather than safe. A mistyped id fails the gate rather than the next amendment — closed on the governance path is the worst place to find a spelling mistake | consumer_traced | tests/test_permanent_clauses.py::test_withdrawing_a_permanent_clause_is_refused | newz/evidence/hard_core.py | `withdrawn_permanent(new_ids)` returns what a proposed constitution would drop and `tools/amend_constitution.py` exits 2 on any; `validate()` fails the gate when a permanent id is absent from the newest `constitution/vN.yaml` |
| INV-112 | The resolver reads what the being already has (E1.8). `default_adapters()` indexes Wikipedia, arXiv, OpenAlex, PubMed and SEC EDGAR and none of them is an RSS feed, so the being's 61 subscribed sources were invisible to the one mechanism that can tell it that it was wrong. Measured 2026-08-28: claim 22 names *"Federal Open Market Committee (FOMC) Statement or Meeting Minutes"* as its resolver and those minutes were in `harvest_log` from 2026-08-19, offered and unread, while the resolution pass returned Wikipedia's article ABOUT the release. **The being does not name unreachable sources; the resolver looked somewhere else.** `HarvestAdapter` goes FIRST because `research()` dedupes by url in adapter order, so the being's own subscribed source is the one that survives when two adapters return the same document. It ranks and does not decide: the 0.35 floor, triage, the share caps and INV-047's verbatim check all still stand between a candidate and a settled claim. **Resolution only** — a feed item already reaches the being through E1.0's menu, and offering it on both paths would hand the same item through two doors and count it twice against §9.1's diet. The publisher must match and not merely the headline, or a Bloomberg column outranks the Federal Reserve's own feed; function words are not a match, or the feed "The Hindu" answers every claim ever written. | consumer_traced | tests/test_harvest_adapter.py::test_the_resolution_pass_puts_the_harvest_first | newz/world/harvest.py | No schema. The epic stays OPEN: its first clause is in-life — settled from an item that feed delivered — and no test closes that. E1.3 was classed mechanical and closed while its own path had never once executed, which is the fault this answers. |
| INV-113 | A claim can be about what is already true (E1.9). Every claim before this was a forecast: `MIN_HORIZON_DAYS = 2` forbade anything settleable now. Measured 2026-08-28, that cost more than it looked: 19 of 31 open claims settle within 60 days — **0.317 externally-graded events a day** against 103 deliberation cycles and 50 advances — and the caps multiply, since a 29.7-day mean horizon against `MAX_OPEN_CLAIMS = 40` puts sustainable throughput at 40/29.7 = **1.35 a day** whatever the being wants. **A horizon of 0 is the kind, derived and never asked for**, so no field can disagree with the date. `MIN_HORIZON_DAYS` stays for everything above zero: a claim due tomorrow is still refused, too soon for a source to have spoken and too late to be about what already happened. **The 2-day floor was a proxy for "not already in the dossier" and the thing it stood for is now checked directly** — `already_in_the_dossier` asks E1.8's harvest what the resolver names and refuses if that url is in `ingest_log` as read. It is local, exact and needs no network, and a miss is not a pass (INV-044): the adapters reach sources the harvest cannot. **Retrodictions do not consume the carrying pool** — inventory divided by latency is the ceiling and a claim that leaves on the next pass has no inventory cost — but they carry their own daily rate, because a door miscalibrated about the past is no better than one miscalibrated about the future. **A full pool is now a refusal row, not a silent decline**: the being was not asked at all, and silence there reads as "it had nothing to claim" when the truth is "it was not allowed to". The daily rate stays a silent decline, deliberately — it is expected to bind on a productive day and says nothing about the being's supply of claims. | consumer_traced | tests/test_claim_kinds.py::test_a_retrodiction_settles_on_the_next_pass | newz/resolutions/door.py | Migration 0045 adds `resolutions.kind` with a CHECK, defaulting to 'forecast' because every row written under a 2-day floor is one in fact. The two kinds carry different epistemics and must never be averaged — E1.10 is where the metric split lands, and until it does, `claims_settled` mixes them. |
| INV-114 | The two kinds of claim are never averaged (E1.10). A forecast tests the being's model of where things are going; a retrodiction tests whether its assertions about the world are true, and a series mixing them would read a change of mechanism as a change in the being — E2.8's "novelty" mistake, which is why `resolutions.kind` exists at all. **`claims_opened` and `claims_settled` are scoped to `kind='forecast'` with NO version bump, and that is correct rather than convenient**: every row written before migration 0045 was a forecast in fact, since `MIN_HORIZON_DAYS = 2` forbade anything settleable now, so the scoped count returns exactly what it returned for every reading already taken and no delta crosses a seam. What would have broken the series is leaving them unscoped and letting them silently begin counting two quantities. `retrodictions_opened` and `retrodictions_settled` carry the other one and start at zero on 2026-08-28 with no history, which is honest rather than a gap. **`consequence_rate` is the one meaning that changed**, to definition 3: consequence is consequence whichever kind produced it — a retrodiction settled against the world is the world telling the being something it did not manufacture, which is exactly §10's item — so a numerator of forecasts alone would understate it. The v2 series ends and `definitions.sync` recorded the seam. Its numerator is a SUM and never a mean, and an unreadable term makes the sum unreadable rather than smaller (INV-044 inside the arithmetic). | consumer_traced | tests/test_claim_kinds.py::test_the_two_series_never_mix | newz/evidence/derived.py | No schema. Touches four files inside the E3.9 freeze — mechanical.py, derived.py, tools/claims.py and instruments.yaml — which is why it shipped as its own act rather than inside E1.9. |
| INV-115 | A question that failed is not asked in the same words (0046). `search_queries` derived terms from the concern statement with no memory of a previous attempt, so the same statement produced the same terms, the adapters returned the same ranked rows, R1's dedup removed whatever had been read on pass one, and triage correctly refused the tail. Measured 2026-08-30: **146 gaps across 29 distinct questions**, one asked **17 times**, with 753 of 761 candidates refused by triage and 8 by the 0.35 floor at an average best relevance of 0.578 — the material was topical and there was nothing good left to keep. R1 (2026-08-14) fixed READING the same source sixteen times; this is ASKING the same question seventeen times and reading nothing. **The terms are a HINT and never a constraint**: the model may return one it was told had failed, and the floor and triage still decide what is read — a query former that refused to repeat could be walked off the subject one pass at a time. **And the entity must survive the angle**, which is the probe's finding rather than a guess: told "not those", the model turned *"EU Digital Services Act amplification definition"* into *"algorithmic transparency technical metrics"* and best relevance fell 0.683 to 0.590, obeying "different" by abandoning the subject. Bounded to 12 terms over 30 days so the hint does not become the prompt, and silent on a store without the column — the memory improves a pass and is never a precondition for one. | consumer_traced | tests/test_question_memory.py::test_the_prompt_insists_on_keeping_the_entity | newz/world/question.py | Migration 0046 adds `source_gaps.searches`, which `ResearchOutcome` has always carried and `record_gap` discarded — the store could say a question failed and never what was asked. Established by `tools/question_probe.py` BEFORE it shipped: terms moved on 4 of 4 questions and surfaced candidates the being had never been offered, including one at 0.65 relevance that had been in the index for all seventeen passes. |
