"""The monitor — the instruments, read on a rhythm, deciding nothing (P4 Phase 3A).

It runs beside the being rather than inside it, because a monitor that lives in
the thing it monitors reports nothing at the moment that matters. It writes its
own database for the same reason: an observation *about* the being is not part
of the being's record, and `newz.db` keeps exactly one writer.

Nothing here judges. No model is called anywhere in this package.
"""
