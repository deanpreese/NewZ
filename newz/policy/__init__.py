"""Versioned executable policy.

`PLAN.md` Phase 0 turns the specification into policy before persistence is
built around ambiguous concepts. Everything in this package is a pure function
of its inputs and the policy version, so an assessment replays.
"""
